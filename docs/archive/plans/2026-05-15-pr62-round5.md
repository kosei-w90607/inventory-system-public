# PR #62 Codex Round 5 対応 plan (v3: plan-rally Round 2 反映)

## Status

- PR: #62 (private archive) Phase 2 8-2 UI-07 CSV 取込み画面
- Branch: `feat/ui-07-csv-import`
- HEAD: `0149a03` (Codex Round 3 対応 commit)
- Round 履歴: R1=P2:1 close (`2f4071c`) → R2=Approve 相当 (機能 `2136146`) → R3=P1:2/P2:1 (`0149a03` で P1-2+P2-1 採用 + P1-1 実証反論) → R4=P1:1/P2:2 (Request Changes 相当、`2026-05-15T17:29:09Z`、本 plan で対応)
- Round 上限 = 4 (機能 PR)、本 R5 突入は memory `feedback-plan-rally-required-before-exit.md` + `feedback-plan-mode-for-fix-proposals.md` 規約圏内 (本 plan + plan-rally Round 1-2 で mitigation、Round 2 致命的 3 + 重要 6 + 軽微 2 反映済)

## Round 4 採否判定

| 指摘 | 検証結果 | 採否 |
|---|---|---|
| **P1-1 不受容** (CP932 fixture じゃない、新証拠 `e7 b2 be...` UTF-8 bytes) | **誤り** (6 軸実証): 1) local `git show 0149a03:tests/fixtures/z004/normal-small.csv | od -An -tx1 | head -1` = `90 b8 8e 5a 93 fa 95 f1` (CP932「精算日報」); 2) GitHub `/git/blobs/3d1227e78bf07fc48fae39d97ef442dfaa2e5d73` (local blob SHA 一致) = 同上 CP932; 3) GitHub `/contents/...` + `Accept: application/vnd.github.raw` = `e7 b2 be e7 ae 97 e6 97 a5 e5 a0 b1` UTF-8 transcoded; 4) Python3 `decode('utf-8', errors='strict')` = `UnicodeDecodeError: 'utf-8' codec can't decode byte 0x90 in position 0`; 5) GitHub Docs `https://docs.github.com/en/rest/repos/contents` は `/contents/` API の transcoding 仕様を明示せず (WebFetch 実証「該当記述なし」)、ただし実 API 挙動 (2 vs 3) で乖離; 6) **5 fixture 全件 raw blob 実証**: `for f in tests/fixtures/z004/*.csv; do git show 0149a03:$f | od -An -tx1 -N 8 | head -1; done` = 全 5 fixture が `90 b8 8e 5a 93 fa 95 f1` (CP932) で開始、UTF-8 bytes (`e7 b2 be...`) は 1 件も出現せず → Codex「少なくとも 4 fixture が UTF-8」主張完全反証 (plan-rally Round 2 致命的 #3 + 重要 #4 反映) | **再反論** (memory `feedback-codex-p1-empirical-defense.md`) |
| **P2 観点 1** (PowerShell 用 Step 1/2 欠) | 主張正: README L65-86 Step 0 で `$env:DB_PATH` 定義済だが Step 1/2 が CMD + bash のみで PowerShell 経路欠落 | **採用** (Step 1 は引数渡し、Step 2 は `<` 未サポート対応で `Get-Content` パイプ使用) |
| **P2 観点 2** (R119 説明 L174 matched 4 残存) | 主張正: 返品行追加で matched 5 に更新 (L31/L53/L55) だが R119 検証フロー L174 だけ旧表現残存。drift grep 結果 README L174 単独確定 | **採用** |

## Goal

Round 5 で Codex Approve 相当を獲得し、PR #62 を親 plan `docs/plans/2026-05-15-3-pr-progression.md` Track 3 B8 (squash merge + post-merge sync) に遷移可能な状態にする。

## Approach

1. **P1-1 再反論**: 6 軸実証 (上記表参照) + GitHub Docs `/contents/` API transcoding 仕様未明示の事実 + 5 fixture 全件 raw blob CP932 一致 を PR comment に列挙。docs 修正不要。
2. **P2 観点 1 採用**: README Step 1/2 に PowerShell 経路追加 (`Get-Content` パイプ、`<` 未サポート対応、plan-rally Round 1 致命的 #1)。
3. **P2 観点 2 採用**: README L174 R119 検証フロー 4 番を matched 5 に更新 (R119 scope 内に限定、Round 1 重要 #4)。
4. **memory 新規候補**: `feedback-github-contents-api-utf8-transcoding.md` を Track 3 post-merge sync で追加。永続化 anchor を Plans.md L38 + 親 plan `docs/plans/2026-05-15-3-pr-progression.md` Track 3 B8 (L367) + post-merge sync 詳細 (L662 周辺) に紐付け (Round 2 重要 #6 + #7 反映)。
5. **Round 5 Codex プロンプト生成**: skill `/prompt-amplifier` local 不在のため Step 6a で Claude が template 骨格を本 plan 内に出力、Step 6b で user が Codex Desktop に投入 (Round 2 重要 #5 + #8)。

## Steps

### Step 1: P2 観点 1 採用 (README PowerShell Step 1/2 追加)

- ファイル: `tests/fixtures/z004/README.md` L70-90 周辺 (Step 1/2 セクション)
- Step 1 PowerShell patch (引数渡し、PowerShell 5.1/7.x 両方 OK):

```ps
cargo run --bin seed_demo_data -- --db "$env:DB_PATH" --reset
```

> 注記: `--reset` 指定時は `confirm_reset` (`src-tauri/src/bin/seed_demo_data.rs` L196-208) で tty 確認 prompt `本当に reset しますか？ (yes/no):` が表示される。`yes` 入力で続行 (plan-rally Round 2 軽微 #11 反映、CMD/bash 経路も同挙動だが README に明示なかった既知挙動)。

- Step 2 PowerShell patch (**Get-Content パイプ、`<` 演算子未サポート対応**、plan-rally Round 1 致命的 #1):

```ps
Get-Content tests\fixtures\z004\setup-products.sql | sqlite3 "$env:DB_PATH"
```

### Step 2: P2 観点 2 採用 (README L174 R119 matched 数値更新)

- 前置 drift grep (memory `feedback-codex-drift-fix-grep-all-locations.md`): `rg -n 'matched 4|matched 5' tests/ docs/` で全箇所列挙、Round 3 返品行追加で drift は L174 単独と実証済
- ファイル: `tests/fixtures/z004/README.md` **L174** (R119 検証フロー 4 番、section header L166)
- 旧: `commit 押下で csv_import_errors テーブルに 1 行記録、sale_records / inventory_movements には記録されない (matched 4 商品のみ取込み)`
- 新: `commit 押下で csv_import_errors テーブルに 1 行記録、sale_records / inventory_movements には記録されない (matched 5 行のみ取込み、販売 4 + 返品 1)` (R119 scope 内、返品 net +2 詳細は L31/L53/L55 で既述)

### Step 3: doc-consistency + drift keyword 検証

- `./scripts/doc-consistency-check.sh` 19 項目 pass + R3 link 262 件 pass
- 追加 keyword 検証 (本 README が doc-consistency 対象外のため補強、Round 1 重要 #7):
  - `rg "matched 4" tests/fixtures/z004/README.md` で 0 件
  - `rg "matched 5" tests/fixtures/z004/README.md` で適正箇所のみ (L31/L53/L55/L174)
  - `rg --glob '!**/2026-05-15-pr62-round5.md' "Round 4 待ち" docs/` で 0 件 (Plans.md sync 完了確認、Step 4 後、**`**/` で archive 移送後 `docs/archive/plans/...` も除外**、plan-rally Round 2 致命的 #2 + Round 3 致命的 #1)。Step 7 B8 archive 移送後はこの verification command は retire (本 plan が pre-merge 状態でしか keyword sync を検証しないため、archive 後は親 plan 側で別途検証)

### Step 4: commit + push (Plans.md L23 sync 同梱 + 親 plan 追記、plan-rally Round 1 致命的 #3 + Round 2 重要 #7 反映)

- 対象差分: 
  - `tests/fixtures/z004/README.md` (Step 1/2 PowerShell + L174 R119)
  - `docs/Plans.md` **L23 内 2 箇所**の sync (Round 2 軽微 #10 反映):
    - `R3 (...P1-1 実証反論 PR comment 投稿、Round 4 待ち)` → `R3 (...P1-1 実証反論 PR comment 投稿) → R4=Request Changes 相当 P1:1/P2:2 (2026-05-15T17:29:09Z、P1-1 不受容で 5 fixture 全件 raw blob 実証で再反論、P2:2 採用、Round 5 待ち)`
    - `Codex Round 4 確認 待ち` → `Codex Round 5 確認 待ち`
  - `docs/plans/2026-05-15-3-pr-progression.md` Track 3 B8 周辺 (L367 + L662 周辺) に**本 plan archive 移送 + memory 新規 anchor `feedback-github-contents-api-utf8-transcoding.md` 追加**の 1 行追記 (Round 2 重要 #7 反映、宙に浮き防止)
- commit message (plan-rally Round 3 重要 #3 反映、conventional commit 72 字推奨遵守):
  - subject (72 字以内): `fix(ui-07): Codex Round 4 P2 採用 (PowerShell + R119 matched 数値)`
  - body (HEREDOC で複数行):
    ```
    - PowerShell Step 1/2 追加 (Get-Content パイプ、`<` 未サポート対応)
    - R119 検証フロー L174 matched 4 → matched 5 (販売 4 + 返品 1)
    - Plans.md L23 sync 同梱 (Round 4 待ち → Round 5 待ち、2 箇所)
    - 親 plan Track 3 B8 + post-merge sync 詳細に本 plan archive 移送 + memory `feedback-github-contents-api-utf8-transcoding.md` anchor 追記
    - P1-1 再反論は本 commit に docs 修正なし、PR comment で 6 軸実証 (5 fixture 全件 /git/blobs/ 証拠)
    ```
- push 先: `feat/ui-07-csv-import`

### Step 5: PR #62 reply (P1-1 再反論 + P2 採用報告)

- 内容構造:
  - **P1-1 再反論実証 (6 軸)** (Round 4 採否判定表と同内容を PR comment markdown で展開):
    1. local `git show 0149a03:tests/fixtures/z004/normal-small.csv | od -An -tx1 | head -1` = `90 b8 8e 5a 93 fa 95 f1` (CP932)
    2. GitHub `/git/blobs/3d1227e78bf07fc48fae39d97ef442dfaa2e5d73` = 同上 CP932
    3. GitHub `/contents/...` + raw media type = `e7 b2 be...` UTF-8 transcoded (Codex が見た経路)
    4. Python3 `decode('utf-8', errors='strict')` = `UnicodeDecodeError: 'utf-8' codec can't decode byte 0x90`
    5. GitHub Docs `/contents/` API transcoding 仕様未明示 (WebFetch 実証、ただし実 API 挙動で 2 vs 3 乖離)
    6. **5 fixture 全件 raw blob 実証**: `for f in tests/fixtures/z004/*.csv; do git show 0149a03:$f | od -An -tx1 -N 8 | head -1; done` = 全 5 fixture が `90 b8 8e 5a 93 fa 95 f1` (CP932「精算日報」) で開始、UTF-8 bytes 1 件も出現せず
    結論: 実 git blob は CP932 維持 (5 fixture 全件)、Codex の「少なくとも 4 fixture が UTF-8」主張は `/contents/` API の text auto-transcoding 経路を見たことに起因する誤判定。raw blob 確認は `/git/blobs/{sha}` 経由が確実
  - **P2 観点 1 採用**: PowerShell Step 1/2 追加 (Get-Content パイプ使用、`<` redirection 未サポート対応)
  - **P2 観点 2 採用**: R119 matched 数値更新 (L174、R119 scope 内に限定)
  - **Round 5 起動依頼**

### Step 6a: Codex Round 5 プロンプト骨格生成 (Claude、plan-rally Round 2 重要 #5 + #8 反映)

skill `/prompt-amplifier` local 不在のため、Claude が template 骨格を本 plan に出力。user が Codex Desktop に貼り付け可能な完成形に近い粒度で書く。

**template 骨格雛形** (Step 5 完了後 = 新 commit SHA 確定後に最終化):

```
# Role
あなたは Casio POS Z004 売上日報の業務仕様 + Tauri 業務アプリの fixture 設計 + Rust IO 層パーサテスト + Git/GitHub API encoding 整合性 の 4 軸に明るい senior reviewer。

# Task
PR #62 (feat/ui-07-csv-import) の HEAD `<新 commit SHA、Step 4 push 後確定>` で push された Round 4 対応 commit を Round 5 として確認 review せよ。Round 上限 4 を 1 段超過した状態のため、blocker (P1) のみに絞り、major (P2) 以下は記録のみ。

# Context
- repo: private archive, PR #62, base main, branch feat/ui-07-csv-import
- HEAD: <新 commit SHA>
- Round 履歴:
  - R1: P2:1 close (`2f4071c`)
  - R2: Approve 相当 (機能 `2136146`、fixture push 前)
  - R3: P1:2/P2:1 (`0149a03` で P1-2+P2-1 採用 + P1-1 実証反論)、reply #issuecomment-4461800359 (private archive PR #62)
  - R4: P1:1/P2:2 (Request Changes 相当 `2026-05-15T17:29:09Z`、P1-1 反論不受容)、reply #issuecomment-<Step 5 投稿後の ID> (private archive PR #62)
- 本 commit (R4 対応):
  - P1-1 再反論 (6 軸実証、PR comment 別投稿、docs 修正なし)
  - P2 観点 1 採用 (PowerShell Step 1/2 追加、`Get-Content` パイプ)
  - P2 観点 2 採用 (R119 L174 matched 5)

# Constraints (freeze 領域)
- Round 2 以前の機能 commit (`2136146` 以前)
- Round 1 P2-1 fix (`2f4071c`)
- **Round 3 採用 P1-2 (DB 統合 Step 0)、Round 3 採用 P2-1 (返品行追加)** — 再指摘禁止
- **Round 4 採用 P2:2 (本 commit で対応)** — 再指摘禁止
- **Round 4 P1-1 (本 commit で 6 軸実証反論済、`/git/blobs/` raw blob 経路で 5 fixture 全件 CP932 確定、`/contents/` API transcoding は GitHub Docs 未明示の挙動)** — 再指摘するなら GitHub Docs の transcoding 仕様明文 or `/git/blobs/` 経路を覆す新証拠が必要、それ未満なら受容
- PR description / Plans.md 等 docs 更新部分
- doc-consistency 19 項目通過範囲

# Output Routing (default = inline 投稿)
レビュー結果は PR の該当行に inline review comment として直接投稿。投稿不可な環境のみフォールバックで本文 Markdown (出力先頭で宣言)。

**GitHub 制約注意 (self-review)**: PR author = reviewer (kosei-w90607) のため APPROVE / REQUEST_CHANGES API 拒否、review state は COMMENT で投稿し本文に「Approve 相当 / Request Changes 相当」明示。

# Output Format
## 投稿 path (default)
本文構造:
Round 5 review: {Approve 相当 / Request Changes 相当}。GitHub 同一 author/reviewer 制約により COMMENT で投稿します。
P1: N 件 / P2: N 件 / P3: N 件
Round 4 P1-1 反論受容判定: {受容 / 不受容 + 新証拠}
3 観点通過確認:
1. P2 観点 1 採用 (PowerShell): 通過 / 指摘あり
2. P2 観点 2 採用 (R119 matched 数値): 通過 / 指摘あり
3. P1-1 再反論 (6 軸実証 + 5 fixture 全件 raw blob): 受容 / 不受容

# Success Criteria
- P1/P2/P3 件数明示、3 観点通過確認、Round 4 P1-1 反論受容判定必須
- freeze 領域再指摘禁止 (混入していたら self-retract)
- Round 上限 1 段超過のため P1 のみ修正要請、P2 以下は記録

# 3 観点
1. **P2 観点 1 採用 (PowerShell Step 1/2)**: `Get-Content tests\fixtures\z004\setup-products.sql | sqlite3 "$env:DB_PATH"` が PowerShell 5.1/7.x で実行可能か、`$env:DB_PATH` 表記が標準か
2. **P2 観点 2 採用 (R119 L174)**: `matched 4 → matched 5 (販売 4 + 返品 1)` 数値更新が L31/L53/L55 と整合するか、R119 scope を越えて返品 net +2 詳細を持ち込んでいないか
3. **P1-1 再反論 (6 軸実証)**: 6 軸全部 (local git show + /git/blobs/ + /contents/ raw + Python utf-8 strict + GitHub Docs 未明示 + 5 fixture 全件 raw blob) を踏まえて、本 fixture が CP932 で正しく保存されている事実を受容するか。新証拠 (例: GitHub Docs の transcoding 仕様明文 / `/git/blobs/` 経路を覆す proof) を提示するか
```

### Step 6b: user による Codex Desktop 投入 (Round 5 起動)

- **placeholder 取得順序** (Step 5 完了確認後、plan-rally Round 3 致命的 #2 反映):
  1. `git rev-parse HEAD` で新 commit SHA 取得 (Step 4 push 後)
  2. `gh pr view 62 --json comments --jq '.comments[-1] | {id, url}'` で Step 5 PR reply の #issuecomment ID + URL 取得
  3. 取得した SHA / ID を template の `<新 commit SHA>` (Step 6a L102, L106) + `<Step 5 投稿後の ID>` (L111) に置換
- user が完成 template を Codex Desktop に貼り付け、Round 5 起動
- 失敗時 fallback: Plans.md L23 Round 5 待ち記録 + PR description 末尾 trace から手動組立

### Step 7: Round 5 結果対応 (R2 decision tree 連動、plan-rally Round 1 重要 #8)

- **Approve 相当受領** → 親 plan `docs/plans/2026-05-15-3-pr-progression.md` Track 3 **B8 (squash merge + post-merge sync)** へ遷移 (plan-rally Round 2 致命的 #1 反映)。**順序明示** (plan-rally Round 3 重要 #5 反映): (1) user 明示承認 (AskUserQuestion) → (2) `gh pr merge 62 --squash --delete-branch` 実行 → (3) main fetch + checkout → (4) **post-merge sync 内で archive 移送** (squash merge **後** 必須、merge 前実施で PR #62 内 commit 混入リスク): `git mv docs/plans/2026-05-15-pr62-round5.md docs/archive/plans/2026-05-15-pr62-round5.md` + Plans.md L23 sync ([x] + squash hash 記録) + memory `feedback-github-contents-api-utf8-transcoding.md` 追加 (親 plan L662 post-merge sync (iii) + (iv) 準拠)
- **Request Changes 相当** → R2 decision tree (a/b/c) 評価:
  - (a) 本 PR 個別 dissent 記録で merge 強行 (P1 主張誤り実証済の場合、user 判断)
  - (b) fixture encoding 検証ロジックを Vitest 別 PR δ' に分離 (memory `feedback-pr-merge-gate-scope-discipline.md` Plan B)
  - (c) PR #62 close + 新規 PR 開設 (Round 上限 reset)
  - **Round 6 突入時の規約** (本 plan で初定義、plan-rally Round 3 重要 #6 反映):
    - 本 plan v4 は Round 5 専用 scope のため、Round 6 起動は本 plan を close + 新規 plan `docs/plans/2026-05-16-pr62-round6.md` を別途立ち上げ
    - 新規 plan は plan-rally 2 round 以上必須 (memory `feedback-plan-rally-required-before-exit.md` 準拠)
    - **Round 6 起動前に AskUserQuestion で R2 decision tree (a/b/c) を user に提示し、(c) PR close + 新規 PR 開設 を強く推奨** (Round 上限 reset で Codex 自身のコンテキスト汚染も reset、本 PR を 5 round 以上引きずる cost > 新規 PR 開設 cost)

## Verification

各項目末尾の `[実行: Claude / user / 自動]` は Codex Round 5 起動前 (Step 6b 前) に Claude が埋める。user 実走項目は AskUserQuestion で結果回収 (plan-rally Round 3 重要 #4 反映)。

- [ ] **iconv** [実行: Claude]: `iconv -f CP932 -t UTF-8 tests/fixtures/z004/normal-small.csv | head -7` で 6 行全部 decode 成功
- [ ] **file** [実行: Claude]: `file tests/fixtures/z004/normal-small.csv` で `Non-ISO extended-ASCII text, with CRLF, NEL line terminators` 確認
- [ ] **5 fixture raw blob 一致** [実行: Claude]: `for f in tests/fixtures/z004/*.csv; do git show 0149a03:$f | od -An -tx1 -N 8 | head -1; done` で 5 行全部 `90 b8 8e 5a 93 fa 95 f1` (CP932) 開始 (P1-1 再反論の central evidence)
- [ ] **keyword drift (matched)** [実行: Claude]: `rg "matched 4" tests/fixtures/z004/README.md` で 0 件、`rg "matched 5" tests/fixtures/z004/README.md` で適正箇所のみ (L31/L53/L55/L174)
- [ ] **keyword sync (Plans.md、self-pollution 回避)** [実行: Claude]: `rg --glob '!**/2026-05-15-pr62-round5.md' "Round 4 待ち" docs/` で 0 件
- [ ] **親 plan 追記実証** [実行: Claude]: `rg "pr62-round5|github-contents-api-utf8" docs/plans/2026-05-15-3-pr-progression.md` で 2 件以上 hit
- [ ] **PowerShell syntax 実走** [実行: user]: Windows native で `$env:DB_PATH = "$env:APPDATA\com.kosei.inventory\inventory.db"; cargo run --bin seed_demo_data -- --db "$env:DB_PATH" --reset; Get-Content tests\fixtures\z004\setup-products.sql | sqlite3 "$env:DB_PATH"` 実走、結果を AskUserQuestion で回収 (WSL2 では不能)
- [ ] **doc-consistency** [実行: Claude]: `scripts/doc-consistency-check.sh` 19 項目 pass + R3 link 全 pass (本 fixture README は対象外、keyword grep で補強)
- [ ] **pre-push hook** [実行: 自動 (git push 時)]: 本変更 (README + Plans.md + 親 plan) は `scripts/pre-push.sh` の判定対象 (src-tauri/ / docs/function-design/ / src/ / .env) **いずれにも該当せず**、全 check skip + hook exit 0 (Round 3 push 時の `⏭️ 対象ファイルの変更なし、チェックスキップ` と同パターン、frontend / doc-consistency は実走 not green、`skip` 判定)

## Risks

| ID | リスク | 確率 | 影響 | 緩和策 |
|---|---|---|---|---|
| R1 | Round 5 で Codex がさらに別 P1 を出す | 中 | 高 | 本 plan v3 + plan-rally Round 1-2 (致命的 6 + 重要 12 + 軽微 2 反映済) で軽量範囲は full cover。同 PR 完結困難なら R2 decision tree (b/c) 検討 |
| R2 | P1-1 再反論を Codex が再不受容 | 低 (5 fixture 全件 + 6 軸実証で前回より大幅補強) | 高 | **decision tree**: <br>(a) 本 PR 個別 dissent 記録で merge 強行 (user 判断、判定軸 = Codex 主張の新証拠が `/git/blobs/` + 5 fixture 全件証拠を上回るかどうか)<br>(b) fixture encoding 検証ロジックを Vitest 別 PR δ' に分離 (memory `feedback-pr-merge-gate-scope-discipline.md` Plan B)<br>(c) PR #62 close + 新規 PR 開設 (Round 上限 reset)<br>Round 6 突入時点で plan-rally 別ラリー起動 + user に decision tree 提示 |
| R3 | ~~PowerShell `<` 演算子未サポート~~ | (close 済、plan-rally Round 1 致命的 #1) | - | - |
| R4 | Plans.md 200 行超過 (現 202 行) | 中 | 低 | Track 3 post-merge sync で同時 cleanup、Round 5 修正 scope に混ぜない (memory `feedback-pr-merge-gate-scope-discipline.md`) |
| R5 | memory 新規 anchor `feedback-github-contents-api-utf8-transcoding.md` 揮発 | 中 | 低 | **3 重 anchor** (plan-rally Round 2 重要 #6 反映): (i) 本 plan Related セクション、(ii) Plans.md **L38** (post-merge sync bullet 内 memory 軽量監査、L36 ではない)、(iii) 親 plan `docs/plans/2026-05-15-3-pr-progression.md` Track 3 B8 (L367) + post-merge sync 詳細 (L662 周辺) に追記 (Round 2 重要 #7) |

## Related

- 親 plan: `docs/plans/2026-05-13-phase-2-ui-07.md` (Phase 2 8-2 UI-07 実装プラン)
- 3 PR 順次マージ plan: `docs/plans/2026-05-15-3-pr-progression.md` (Track 3 B8 = squash merge + post-merge sync。本 plan archive 移送 + memory 新規 anchor 追記を **Step 4 commit に同梱**、plan-rally Round 2 重要 #7 反映)
- **memory 新規追加 anchor** (Track 3 post-merge sync で追加対象、3 重 anchor):
  - `feedback-github-contents-api-utf8-transcoding.md`: GitHub `/contents/` API は text file の bytes を UTF-8 transcoding して返す (GitHub Docs は明示せず、本 PR Round 3-4 で実証)。真の raw blob は `/git/blobs/{sha}` で取得。Codex / 他 review agent が `/contents/` を見て encoding 判定すると誤判定する罠
- 関連 memory:
  - `feedback-codex-p1-empirical-defense.md` (P1-1 再反論判断軸)
  - `feedback-plan-rally-required-before-exit.md` (Round 5 突入時の plan-rally 規約、本 plan で 2 round 実行)
  - `feedback-plan-mode-for-fix-proposals.md` (Codex review 対応 plan の Plan agent 強制)
  - `feedback-claude-self-bias-blind-spot.md` (Round 3 反論時 `/git/blobs/` 検証抜け + Round 2 critique で本 plan の self-pollution / step 番号誤り発見、plan-rally で救済)
  - `feedback-codex-drift-fix-grep-all-locations.md` (R119 drift 全箇所 grep、L174 単独確定)
  - `feedback-pr-merge-gate-scope-discipline.md` (memory 新規追加 + Plans.md cleanup を本 PR scope に混ぜない判断軸)
  - `feedback-self-trace-expression-breaks-sync-loop.md` (Plans.md L23 sync 同梱判断、機能 commit と docs sync 表記分離)
  - `feedback-self-review-mechanical-addition-anti-pattern.md` (Self-Review 各観点 100 字以上 + 行番号 + memory 参照必須)
  - `plan-self-review-before-implementation.md` (本 plan 7 観点 Self-Review 適用根拠)
- Round 4 review: `gh pr view 62 --json reviews --jq '.reviews[-1]'` (2026-05-15T17:29:09Z)
- Plan rally Round 1 結果: 致命的 3 + 重要 6 + 軽微 3 (v2 で反映)
- Plan rally Round 2 結果: 致命的 3 + 重要 6 + 軽微 2 (本 v3 で全反映)

## Self-Review (7 観点、memory `plan-self-review-before-implementation.md` 適用)

本 plan は「PR レビュー対応 + 複数 step (Step 1-7) + 複数ファイル編集 (README L70-90 + L174 + Plans.md L23 + 親 plan 追記 + PR comment + commit message + Codex template)」のため適用対象。各観点 100 字以上 + blockquote / 行番号 / memory 参照必須 (memory `feedback-self-review-mechanical-addition-anti-pattern.md` 強制、placeholder hits < 2、DISTINCT_TOKENS ≥ 25)。

### 1. 技術的前提

> 本 plan の前提 = 現 branch `feat/ui-07-csv-import` HEAD `0149a03` + Plans.md L23 が Round 3 状態 + 新 commit で Plans.md L23 「Round 4 待ち」2 箇所 + 親 plan Track 3 追記を同梱 (memory `feedback-self-trace-expression-breaks-sync-loop.md`)。

LSP/Skills Policy 適用判断: 編集対象は `tests/fixtures/z004/README.md` (.md) + `docs/Plans.md` (.md) + `docs/plans/2026-05-15-3-pr-progression.md` (.md) + 本 plan ファイル自身で、Rust / TypeScript の semantic 編集ゼロ。memory `feedback-lsp-skills-policy-hook.md` で LSP baseline 取得不要。skills-decision.json は plan-rally 起動時に更新済、Codex Round 5 プロンプト生成は Step 6a で Claude が手動 (Skill tool 起動なし)。rebase 要否: main HEAD `683d24d` (PR #63 merge) 取り込み不要、本 plan 実装 commit は `0149a03` 上で OK。commit prefix `fix(ui-07):` は PR #62 既存 (`2f4071c` / `0149a03`) と一貫、PR squash 後の main 履歴で集約。

### 2. スクリプト詳細

> 本 plan の Bash / PowerShell スクリプトは user 環境で実走想定、Claude 側は手順記述のみ。Step 1 PowerShell の `<` は PowerShell 5.1 で未サポート (plan-rally Round 1 致命的 #1)、`Get-Content ... | sqlite3 ...` パイプ必須。`--reset` は tty 確認 prompt あり (Round 2 軽微 #11)。

具体構文: Step 1 = `cargo run --bin seed_demo_data -- --db "$env:DB_PATH" --reset` (引数渡し、`src-tauri/src/bin/seed_demo_data.rs` L107-114 で `--db <path>` 受付確認済、L196-208 で `confirm_reset` tty 確認、`yes` 入力必須)、Step 2 = `Get-Content tests\fixtures\z004\setup-products.sql | sqlite3 "$env:DB_PATH"` (PowerShell 5.1 互換 `Get-Content` パイプ)。`$env:DB_PATH` は Windows PowerShell 標準。bash `$DB_PATH` + CMD `%DB_PATH%` は既存 README で実証済。grep スクリプト `rg --glob '!plans/2026-05-15-pr62-round5.md' "Round 4 待ち" docs/` は self-pollution 回避必須 (plan-rally Round 2 致命的 #2)、本 plan 自身が keyword 5 箇所保持。5 fixture 全件実証 `for f in tests/fixtures/z004/*.csv; do git show 0149a03:$f | od -An -tx1 -N 8 | head -1; done` は plan-rally Round 2 中に実走済 (Round 2 致命的 #3 + 重要 #4 補強)。

### 3. ドキュメント修正

> 本 plan の docs 修正は 3 ファイル: `tests/fixtures/z004/README.md` (Step 1/2 + L174)、`docs/Plans.md` (L23 内 2 箇所 sync)、`docs/plans/2026-05-15-3-pr-progression.md` (Track 3 B8 L367 + post-merge sync L662 周辺に本 plan archive + memory anchor 追記、plan-rally Round 2 重要 #7)。

編集対象 line の重複/矛盾確認: README L31/L33/L53/L55 + L70-90 + L174 で `matched N` 散在、本 plan は L70-90 + L174 のみ修正、L31/L53/L55 は Round 3 で更新済 drift なし (plan-rally Round 2 中 grep 確認)。Plans.md L23 sync は 2 箇所 (前半 `Round 4 待ち)` + 末尾 `Codex Round 4 確認 待ち`、plan-rally Round 2 軽微 #10) 両方を Step 4 で更新、self-trace 原則 (memory `feedback-self-trace-expression-breaks-sync-loop.md`) で機能 commit に同梱。親 plan 追記は Round 2 重要 #7 で「archive 移送先 + memory 新規 anchor 紐付け」を Step 4 commit に同梱、Track 3 B8 (L367) + post-merge sync 詳細 (L662 周辺) の 2 箇所に 1 行ずつ追記、宙浮き防止。link 参照: 本 plan が `docs/plans/2026-05-13-phase-2-ui-07.md` + `docs/plans/2026-05-15-3-pr-progression.md` + memory MEMORY.md 索引を参照、Round 5 archive 移送時に link 切れ可能性、Track 3 post-merge sync で対応 (archive 後 docs sync で link 更新)。

### 4. 検証計画

> 本 plan の verification は **9 軸**: (a) iconv、(b) file、(c) 5 fixture raw blob 一致 (P1-1 再反論 central evidence、Round 2 重要 #4)、(d) keyword drift `matched`、(e) keyword sync `Round 4 待ち` (glob 除外で self-pollution 回避、Round 2 致命的 #2)、(f) 親 plan 追記実証 (Round 2 重要 #7)、(g) PowerShell syntax (user 依頼)、(h) doc-consistency、(i) pre-push hook (skip 判定明示、Round 2 重要 #9)。

ローカル検証コマンド: `iconv -f CP932 -t UTF-8 tests/fixtures/z004/normal-small.csv | head -7` で 6 行全部 decode、`file ...` で `Non-ISO extended-ASCII text, with CRLF, NEL line terminators`、`for f in tests/fixtures/z004/*.csv; do git show 0149a03:$f | od -An -tx1 -N 8 | head -1; done` で 5 fixture 全件 `90 b8 8e 5a 93 fa 95 f1` (CP932 開始) を確認 (plan-rally Round 2 中実走済)、`rg "matched 4" tests/fixtures/z004/README.md` で 0 件、`rg --glob '!plans/2026-05-15-pr62-round5.md' "Round 4 待ち" docs/` で 0 件、`rg "pr62-round5|github-contents-api-utf8" docs/plans/2026-05-15-3-pr-progression.md` で 2 件以上 hit。CI 予測: PR #62 既存 CI 3 ジョブ全 green。pre-push hook 挙動予測 (plan-rally Round 2 重要 #9 反映): `scripts/pre-push.sh` の判定対象 (src-tauri/ / docs/function-design/ / src/ / .env) いずれにも本変更該当せず、全 check skip + hook exit 0 = `⏭️ 対象ファイルの変更なし、チェックスキップ` 出力 (Round 3 push の `0149a03` と同パターン)。frontend / doc-consistency は実走 not green、skip 判定であることを Verification チェックリストで明示済。

### 5. 後処理

> 後処理 = Round 5 結果対応 (Approve → 親 plan Track 3 B8 遷移、user 承認待ち / Request Changes → R2 decision tree)、本 plan archive 移送 (Track 3 で `docs/archive/plans/2026-05-15-pr62-round5.md`)、memory 新規 `feedback-github-contents-api-utf8-transcoding.md` 追加、sentinel `.last_audit` 更新は Track 3 post-merge sync 完了タイミング。

memory 軽量監査判断基準: 本セッションで新規発見 = (a) GitHub `/contents/` API の UTF-8 transcoding 罠 (memory 新規候補、3 重 anchor で永続化、Track 3 で実体追加)、(b) PowerShell `<` 演算子未サポート (既存 memory なし、本 plan で fix、軽量で memory 化判断は post-merge sync 時)、(c) Plan rally Round 2 で本 plan の self-pollution / step 番号誤り / 親 plan 参照 0 件など structural issue 発見 (memory `feedback-claude-self-bias-blind-spot.md` の生きた実例として追記候補、ただし既存 memory に同趣旨記述あれば skip)。post-merge sync で sentinel `touch .last_audit` 更新 + 軽量監査。プラン archive: 本 plan は Track 3 で archive 移送、Plans.md L23 から link 削除 (Round 5 close 後) + 親 plan Track 3 完了マーク + memory 新規 anchor の Plans.md L38 (post-merge sync bullet 内 memory 軽量監査) 紐付け (plan-rally Round 2 重要 #6 で L36 → L38 訂正)。

### 6. 実行制約

> Claude は本 plan の commit / push 実行可、squash merge / force push / shared state 変更は user 明示承認待ち (CLAUDE.md「Executing actions with care」+ memory `feedback-pr-merge-gate-scope-discipline.md`)。R2 (a) merge 強行も user 判断、本 plan 単独で実行禁止。

具体制約: (1) `gh pr merge 62 --squash --delete-branch` は Step 7 Approve 相当受領後、AskUserQuestion で承認確認 (PR #63 同パターン実証済)、本 plan ではなく**親 plan Track 3 B8** で実施 (plan-rally Round 2 致命的 #1 反映)、(2) `git push --force` 系は本 plan 範囲では使用しない、(3) Plans.md + 親 plan edit は本 commit に同梱、user 確認なし範囲、self-trace 原則準拠、(4) sandbox 制約: `.claude/skills/` は HARD BLOCK で本 plan 編集対象外、`.claude/state/skills-decision.json` は plan-rally 起動時に更新済、(5) pre-push hook bypass (`--no-verify`) 禁止 (CLAUDE.md 安全ルール)、(6) **Step 6a で Claude が template 骨格を本 plan に出力 (Round 2 重要 #5 + #8)、Step 6b で user が Codex Desktop に投入**。Claude は template 生成 + PR comment 投稿 (`gh pr comment 62`) まで、Round 5 起動本体は user の Codex Desktop で実施、(7) memory 新規追加 (`feedback-github-contents-api-utf8-transcoding.md`) は Track 3 post-merge sync で実施、本 PR scope 外。

### 7. コミット分割

> 本 plan の commit は **1 commit 集約** (`fix(ui-07): Codex Round 4 P2 採用 ... + Plans.md L23 + 親 plan Track 3 sync 同梱`)。理由 = (a) README 変更 + Plans.md sync + 親 plan 追記が同一 PR scope 内、(b) self-trace 原則、(c) hook 要求 `[MANDATORY] Plans.md 反映` 同梱 (Round 1 致命的 #3)、(d) 親 plan 追記の宙浮き防止 (Round 2 重要 #7)。

各 commit の scope/依存/prefix: 単一 commit、依存関係なし。prefix `fix(ui-07):` 一貫。**技術 hook 順序** (commit 完了まで、plan-rally Round 3 重要 #7 反映): (1) Edit で README L70-90 + L174 + Plans.md L23 (2 箇所) + 親 plan Track 3 B8 + L662 周辺を順次更新 (LSP/Skills hook 適用外、.md 編集)、(2) `git add tests/fixtures/z004/README.md docs/Plans.md docs/plans/2026-05-15-3-pr-progression.md` でステージ、(3) `git commit` 実行 (subject 72 字 + body HEREDOC)、(4) PostToolUse hook `[MANDATORY] Plans.md 反映` は本 commit 内同梱で satisfy、(5) `git push` で pre-push hook skip 判定 (Round 2 重要 #9 反映)。**Step 進行** (commit 後、hook 範囲外): (6) Step 5 PR comment 投稿、(7) Step 6a template 骨格生成、(8) Step 6b user が Codex Desktop に投入、(9) Step 7 分岐 (Approve / Request Changes / Round 6 → 別 plan)。別 commit 検討: (a) memory 新規追加は Track 3 で別 commit、(b) Plans.md 全体 cleanup (200 行制限) も Track 3 で別 commit、(c) 本 plan archive 移送も Track 3。本 plan 範囲は単一 commit。
