# Phase 2 8-5 UI-09b 月次売上レポート + 8-7 useExportFile 共通化 実装プラン (Round 5 反映済、converged 判定 = self-reference drift ループ回避のため Round 6 省略 + ExitPlanMode 進行)

> **最終配置先**: `docs/plans/2026-05-18-phase-2-ui-09b.md` (ExitPlanMode 後 commit 1 で repo 内へ移送 + 相対パス変換)
> **想定 PR**: `feat/ui-09b-monthly-sales` → PR #66 前後 (直近 PR #65 UI-09a `8c2be51`)
> **memory 参照先**: `~/.claude/projects/-home-kosei-Projects-inventory-system/memory/` 配下
> **plan 配置**: home `/home/kosei/.claude/plans/` 配下 (repo 内 `.claude/plans/` は存在しない、H-8)
> **Plan rally 履歴**:
> - Round 0 = Plan agent 初版 + 実機検証 4 訂正 + user Q-1/2/3
> - Round 1 = F-1〜F-15 (critical 4 + important 6 + minor 5) + user Q-4/Q-5 全反映
> - Round 2 = G-1〜G-15 (critical 3 + important 7 + minor 5) 全反映 + 実機裏取り 2 件
> - Round 3 = H-1〜H-10 (critical 2 + important 4 + minor 4) 全反映 + 実機裏取り 3 件
> - Round 4 = I-1〜I-5 (critical 0 + important 3 + minor 2) 全反映、数値整合 drift 修正
> - Round 5 = J-1〜J-5 (critical 0 + important 3 + minor 2) 全反映、plan top/bottom メタ情報 self-reference drift 修正
> - **Round 6 (本版) = user critique 3 件反映**: P1 critical (Q-6 A 案: `export_sales_csv` CMD signature 変更で SalesReportType fully specta 化、UI-09a wrapper 化で吸収) + P2 important (Q-7: prev_amount <= 0 = 比較不可「—」、Z004 返品月の色分け逆転回避) + P2 important (npx prettier worm 経路回避: `./node_modules/.bin/prettier` または `sed -i` 一本化)。**累積 53 件発見 + 全反映、Plan rally machine-driven 5 round + user human-driven 1 round で converged**

---

## Context

Phase 2 中核 5 画面 4 番目。UI-00 (PR #56) / UI-07 (PR #62) / UI-09a (PR #65) 主動線完成済み。**機械優先採用** (Plans.md L31): BIZ-05 `get_monthly_sales` 実装済 + UI-09a パターン横展開 (Q-5 で 1 useQuery 設計に修正)。**Scope**: UI-09b 本体 + 8-7 `useExportFile` 共通化 + TabsHeader 共通化 + Progress wrapper 配置 + SalesReportType specta 化 (H-5)。

---

## User 確定事項 (Q-1〜Q-5)

(Round 0/1 で確定、詳細省略)

---

## Plan rally Round 1+2 反映 (F-1〜F-15 + G-1〜G-15)

(Round 1/2 で全反映、Round 3 で訂正された G-1 / G-7 path は本版で正される)

---

## Plan rally Round 3 反映 (H-1〜H-10、本版での新規)

- **H-1 (critical)**: G-1 文言訂正 = `SalesMode` `#[serde(rename_all = "snake_case")]` は **response serialize のみ**、`get_monthly_sales(month: String, mode: String)` の request 引数は **String のまま** (sales_cmd.rs L54-65 既存、export_sales_csv 同パターン)。frontend 側 URL state `z.enum(["by_product", "by_department"])` と bindings `getMonthlySales(month, mode: string)` 呼出リテラル値を一致させる。
- **H-2 (critical)**: Plan 全箇所で `FUNCTION_DESIGN.md` → **`docs/FUNCTION_DESIGN.md` (root 直下)** に明示、`docs/function-design/` 配下ではない (個別設計書 20-/30-/40-/50- 配置、索引は root)。L42 UI-09a 「2 useQuery」隣に UI-09b 「1 useQuery + prev_month_comparison」exposition 追加 + L111 UI-09b 索引 1 行追加 = 2 行更新。
- **H-3 (important)**: `formatMonthLabel("2026-01")` UI 表示出力 = **「2026年1月」(zero-pad なし、モックアップ + UI-09a Intl 出力準拠)**、ISO 形式再構築 (`prevMonth`/`nextMonth` 戻り値) のみ `padStart(2, "0")` 適用。format-month-label.test に `formatMonthLabel("2026-01")` → `"2026年1月"` + `formatMonthLabel("2026-12")` → `"2026年12月"` 明示。
- **H-4 (important)**: worm advisory 判定式 = `gh api /advisories/GHSA-g7cv-rxg3-hmpx --jq '{state, withdrawn_at, published_at}'` 実行、**`.withdrawn_at == null` (撤回されていない) なら警戒継続**、state が null/published いずれでも未撤回扱い。archive plan `2026-05-17-vitest-install-safety.md` L128-140 の jq + bash 判定パターン引用。
- **H-5 (important)**: `SalesReportType` enum specta 化 (A 案、specta 化対象 5 → **6 点拡張**) = `#[derive(specta::Type)] + #[serde(rename_all = "snake_case")]` 付与で bindings に `type SalesReportType = "daily" | "monthly_by_product" | "monthly_by_department"` 出力、useExportFile.ts から import (drift 耐性 + frontend/Rust 一元化)。B 案 (frontend 独自 literal 定義 + drift コメント) は却下 (二重管理リスク)。
- **H-6 (important)**: screen_mockups.html drift 3 件 (Q-1 営業日数 / Q-3 tab ラベル / Q-4 部門フィルタ) の post-merge note 集約 = **archive plan `2026-05-18-phase-2-ui-09b.md` 末尾に 3 行 note**「UI-09b 実装で screen_mockups.html L398-404 の Q-1 営業日数 / Q-3 tab ラベル / Q-4 部門フィルタ 3 件が drift、関数設計 57-ui-monthly-sales.md / SCREEN_DESIGN.md 月次節を正、mockup は historical reference として不変」
- **H-7 (minor)**: Plans.md L107 PR #65 メモ末尾に **「Sonner id は UI-09b で `export-daily-*` に rename (`-csv-` セグメント削除)、本 PR 当時の `export-daily-csv-success`/`export-daily-csv-error` は history reference」1 句追記**
- **H-8 (minor)**: Plan §8.1 #13 に「plan は repo 外の `/home/kosei/.claude/plans/` 配置 (home `.claude/`)、**repo 内 `.claude/plans/` は存在しない方針**、commit 1 で `docs/plans/2026-05-18-phase-2-ui-09b.md` 移送後は repo 内」明示
- **H-9 (minor)**: Plan §8.2 F-11 Windows native 検証項目末尾に「**IME 確認は本画面では不要 (日本語テキスト入力 field 0 件、`<input type="month">` + Select + Button + Tooltip のみ)**」(memory `tauri2-linux-ime-limitation.md` 整合)
- **H-10 (minor)**: Plan §9 G-10 注記末尾に「commit 5 (lib + types + hooks) は route 非依存だが、commit 6 (route 配線 + routeTree.gen.ts 自動再生成) を待たない単独 typecheck は `routeTree.gen.ts` 旧版で動くため fail 可能、squash merge 後の最終 state のみ pass 担保」1 句追加

---

## 実機検証で訂正済み 4 + Round 2 検証 2 + Round 3 検証 3 = 9 点

1-6. (Round 1/2 で確定)
7. **(Round 3 検証) H-1**: `sales_cmd.rs:54-65` で `mode: String` 受け + match 変換 (export_sales_csv 同パターン)、request 引数 String 確定
8. **(Round 3 検証) H-2**: `docs/FUNCTION_DESIGN.md` (root 直下)、`docs/function-design/` 配下は個別設計書のみ
9. **(Round 3 検証) H-4**: `gh api /advisories/GHSA-g7cv-rxg3-hmpx --jq '{state, withdrawn_at}'` 実機戻り値 = `{"state":null,"withdrawn_at":null}` → 判定式は `withdrawn_at` で

---

## §1 関数設計書 `docs/function-design/57-ui-monthly-sales.md` (新規、10 章 480-560 行)

### 1.1 テンプレ判定: 業務ロジックあり版簡潔版 (1 useQuery、Q-5)

### 1.2 サマリ 4 カード (Q-1 + Q-5)
1. 月間売上合計 / 2. 月間販売点数 / 3. 「期間: 2026/03/01-03/31」固定文言 / 4. 前月比 (prev_month_comparison 空配列 = 「比較不可」)

### 1.3 表示要素 (Q-4 反映、G-3 1 位バッジ ranking 追従)
- TabsHeader 共通 (`<Link>` 日次/月次)
- ModeTabs (`?mode=by_product|by_department`)
- 部門別テーブル 5 列 / 商品ランキング 6 列 (Q-4)
- 1 位黄色バッジは `item.ranking === 1` 追従 (G-3)
- CSV 出力 + 印刷 disabled (Tooltip)

### 1.4 ファイル構成 (新規 20 + 移送 1 + 更新 5 = 26 file、本表は本体のみ、Vitest 7 file は §7 列挙、commit 3 削除 1 は更新枠に括り込み、J-4 連動更新)

| 配置 | ファイル | 責務 |
|---|---|---|
| 新規 | `src/routes/reports/monthly.tsx` | route + zod 4 validateSearch |
| 新規 | `src/features/monthly-sales/MonthlySalesPage.tsx` | 最上位 + Alert/Empty (Q-5) |
| 新規 | `src/features/monthly-sales/types.ts` | SortColumn/SortDirection/SalesViewMode/ProductRankingRow/DeptCompositionRow |
| 新規 | `src/features/monthly-sales/lib/compute-summary.ts` | total amount/quantity |
| 新規 | `src/features/monthly-sales/lib/compute-period-label.ts` | 「2026/03/01-03/31」(F-10 + G-9) |
| 新規 | `src/features/monthly-sales/lib/compute-comparison.ts` | key 突合 + 0 ガード (Q-5) |
| 新規 | `src/features/monthly-sales/lib/compute-composition.ts` | 部門別構成比 % |
| 新規 | `src/features/monthly-sales/lib/pick-top-ranking.ts` | 上位 10 (BIZ row_number 同順位なし前提) |
| 新規 | `src/features/monthly-sales/lib/sort-items.ts` | 列ソート + null 末尾 |
| 新規 | `src/features/monthly-sales/lib/format-month-label.ts` | **H-3: UI 表示「2026年1月」zero-pad なし、ISO 再構築 `padStart(2, "0")`** |
| 新規 | `src/features/monthly-sales/lib/test-fixtures.ts` | 2 factory (G-12) |
| 新規 | `src/features/monthly-sales/hooks/useMonthlySalesReport.ts` | 1 useQuery (Q-5) |
| 新規 | `src/features/monthly-sales/components/MonthNavigator.tsx` | 前月 + `<input type="month">` + 翌月 |
| 新規 | `src/features/monthly-sales/components/ModeTabs.tsx` | mode 切替 |
| 新規 | `src/features/monthly-sales/components/SummaryCardsBar.tsx` | 4 カード |
| 新規 | `src/features/monthly-sales/components/DepartmentTable.tsx` | 5 列 + Progress + 前月比色分け |
| 新規 | `src/features/monthly-sales/components/ProductRankingTable.tsx` | 上位 10 + ranking バッジ (G-3) |
| 新規 | `src/features/monthly-sales/components/ExportBar.tsx` | CSV 出力 + 印刷 disabled |
| 新規 | `src/components/ui/progress.tsx` | **G-2: `import { Progress as ProgressPrimitive } from "radix-ui"` 統合パッケージ** |
| 移送 | `src/components/sales/TabsHeader.tsx` | router-driven |
| 新規 | `src/lib/hooks/useExportFile.ts` | **H-5: `SalesReportType` を bindings から import**、G-4 try/catch + private 移送 |
| 更新 | `src/features/daily-sales/components/TabsHeader.tsx` | 削除 |
| 更新 | `src/features/daily-sales/hooks/useExportDailySalesCsv.ts` | wrapper 化 (Q-2) |
| 更新 | `src/routes/reports/daily.tsx` | TabsHeader import + props |
| 更新 | `src/config/navigation.ts` ui-09b ブロック (anchor 文字列、G-5 実機 L93/L95) | `to`/`status` 更新 |
| 更新 | `src/lib/query-keys.ts` | `monthlySales` helper |

### 1.5 章構成 10 章 (Round 1/2/3 反映、詳細省略)

### 1.6 月選択 UI = HTML5 `<input type="month">` (F-11 Windows native + H-9 IME 不要明示)

### 1.7 構成比バーチャート = shadcn `<Progress>` (G-2 統合パッケージ慣習)

### 1.8 前月比色分け閾値 ±1.0% (F-15) + Q-7 prev_amount <= 0 ガード (user P2 反映)

- 緑 +1.0% 以上 / 灰 ±1.0% / 赤 -1.0% 以下
- **「比較不可」灰「—」** (3 条件で発生):
  1. `prev_month_comparison` が空配列 (Q-5)
  2. **prev_amount = 0** (除算ガード)
  3. **prev_amount < 0** (Z004 返品超過でマイナスの月、(current - prev) / prev で色分け逆転回避、Q-7 user 確定)
- 業務上「返品多い月の前月比」は数値比較困難なため、symbolic 表示で利用者に異常値を示唆

### 1.9 営業日数 Q-1 B 案「期間」固定文言

### 1.10 表記揺れ + G-3 ranking バッジ追従 + **H-3 UI 表示フォーマット「YYYY年M月」 (zero-pad なし、モックアップ + UI-09a Intl 出力準拠)、ISO 形式再構築のみ `padStart(2, "0")` 適用**

---

## §2 specta 化作業 (H-5 で 5 → 6 点拡張)

### 2.1 罠 1 防御: 設計書 §57.2 を書く前に specta 実出力先取り確認

### 2.2 specta 化対象 **6 点 + CMD signature 変更 1 点 = 7 点** (Round 5+ user P1 Q-6 A 案反映)

| ファイル:行 | 変更 |
|---|---|
| `src-tauri/src/biz/sales_service.rs:20` | `SalesMode` enum に `specta::Type` + **`#[serde(rename_all = "snake_case")]`** (H-1 response serialize のみ、`get_monthly_sales` request 引数は String のまま) |
| `src-tauri/src/biz/sales_service.rs:64` | `MonthlySalesReport` struct に `specta::Type` |
| `src-tauri/src/biz/sales_service.rs:73` | `MonthlySaleItem` struct に `specta::Type` |
| **`src-tauri/src/biz/sales_service.rs:83`** | **Q-6 A 案 (user P1 反映): `SalesReportType` enum に `specta::Type` + `serde::Deserialize` + `#[serde(rename_all = "snake_case")]` (現状 `#[derive(Debug, Clone)]` のみ、Deserialize 含む全 derive を一括追加)** |
| `src-tauri/src/cmd/sales_cmd.rs:51` | `get_monthly_sales` 関数に `#[specta::specta]` |
| **`src-tauri/src/cmd/sales_cmd.rs:79`** | **Q-6 A 案 (user P1 反映): `export_sales_csv(report_type: String, target: String)` → `export_sales_csv(report_type: SalesReportType, target: String)` CMD signature 変更**、内部 `match report_type.as_str()` を削除 + Rust 側で直接 `SalesReportType` enum 受け取り |
| `src-tauri/src/lib.rs` | `collect_commands![]` に `cmd::sales_cmd::get_monthly_sales,` 追加 + doc 1 行更新 |
| `src/lib/bindings.ts` | `cargo run --manifest-path src-tauri/Cargo.toml --bin generate_bindings` で再生成、**bindings.ts:58 `exportSalesCsv(reportType: string)` → `exportSalesCsv(reportType: SalesReportType)` literal union 化** (`SalesReportType = "daily" \| "monthly_by_product" \| "monthly_by_department"`) |
| `src-tauri/tests/design_compliance_test.rs::SKIP_DOCS` | `57-ui-monthly-sales.md` 追加 |
| **`src/features/daily-sales/hooks/useExportDailySalesCsv.ts`** | **wrapper 化で `exportFile({ reportType: "daily", target: args.date })` の `"daily"` literal を `SalesReportType` 型整合化 (TypeScript 型推論で自動)** |

**H-1 重要事項**: `get_monthly_sales(month: String, mode: String)` の **request 引数は String のまま** (sales_cmd.rs:54-65 `match mode.as_str()` 既存、UI-09b PR では touch しない)。一方 **`export_sales_csv` だけは Q-6 A 案で SalesReportType 型に変更** (UI-09a 既存呼出 + UI-09b 新規呼出が wrapper 化で吸収、drift 耐性最大化)。`#[serde(rename_all = "snake_case")]` は SalesMode/SalesReportType enum 両方の serialize/deserialize 出力を snake_case literal union 化、frontend URL state `z.enum(["by_product", "by_department"])` + bindings 型一致を保証。

### 2.3 検証フロー (F-5 trim + G-11 cwd + G-1 snake_case 確認)
1. `cargo run --manifest-path src-tauri/Cargo.toml --bin generate_bindings`
2. `rg -n -e "MonthlySaleItem" -e "MonthlySalesReport" -e "SalesMode" -e "SalesReportType" -e "getMonthlySales" src/lib/bindings.ts` で実態確認
3. **G-1 検証**: `rg -n -e "by_product" -e "by_department" src/lib/bindings.ts` で snake_case literal union 出力確認 (PascalCase なら反映漏れ)
4. **F-5 trim** (user P2 反映、worm published 継続中で npx 経路回避): `./node_modules/.bin/prettier --write --ignore-path /dev/null src/lib/bindings.ts` (local 解決一本化) または `sed -i 's/[[:space:]]*$//' src/lib/bindings.ts` (npx 不使用、worm advisory 整合)
5. `git diff src/lib/bindings.ts` で expected diff 目視
6. L1 + L2 + clippy pass

---

## §3 8-7 `useExportFile` 共通化 (G-4 try/catch + H-5 SalesReportType import)

### 3.1 新規 `src/lib/hooks/useExportFile.ts` (single hook、80-100 行)

```ts
import { useMutation } from "@tanstack/react-query";
import { commands, type SalesExportResponse, type SalesReportType } from "@/lib/bindings";  // ← H-5: SalesReportType を bindings から import
import { unwrapResult } from "@/lib/invoke";
import { toast } from "sonner";

const REVOKE_DELAY_MS = 100;

export interface ExportFileArgs {
  reportType: SalesReportType;  // ← H-5: bindings 由来の literal union
  target: string;
}

const REPORT_LABEL: Record<SalesReportType, string> = {
  daily: "日次売上",
  monthly_by_product: "月次売上（商品別）",
  monthly_by_department: "月次売上（部門別）",
};

export function useExportFile() {
  const mutation = useMutation<SalesExportResponse, Error, ExportFileArgs>({
    mutationFn: ({ reportType, target }) =>
      unwrapResult(commands.exportSalesCsv(reportType, target), { source: "commands", cmd: "export_sales_csv" }),
    onSuccess: (data, args) => {
      try {                                                     // ← G-4
        downloadBlobFromBase64(data);
        toast.success(`${REPORT_LABEL[args.reportType]} を保存しました（${data.record_count} 件）`, {
          id: `export-${args.reportType}-success`,
        });
      } catch (e) {
        const message = e instanceof Error ? e.message : String(e);
        toast.error(`出力に失敗しました: ${message}`, { id: `export-${args.reportType}-error` });
      }
    },
    onError: (error, args) => toast.error(`出力に失敗しました: ${error.message}`, {
      id: `export-${args.reportType}-error`,
    }),
  });
  return { exportFile: mutation.mutate, isExporting: mutation.isPending };
}

function downloadBlobFromBase64(data: SalesExportResponse): void { /* 既存実装、UI-09a useExportDailySalesCsv.ts L55-74 から移送 */ }
```

### 3.2 UI-09a wrapper 化 (G-4 関数本体削除)

```ts
import { useExportFile } from "@/lib/hooks/useExportFile";

export function useExportDailySalesCsv() {
  const { exportFile, isExporting } = useExportFile();
  return {
    exportCsv: (args: { date: string }) => exportFile({ reportType: "daily", target: args.date }),
    isExporting,
  };
}
```

### 3.3 Sonner id drift (Q-2 + F-14 + H-7)
- 確定同期: 関数設計 56-ui-daily-sales.md L232-233/L335-336 + 実装 wrapper 化で旧 id 削除 + **H-7: Plans.md L107 PR #65 メモ末尾 1 句追記**
- F-14 不変: archive plan 末尾 post-merge note 1 行 + screen_mockups.html 不変

---

## §4 URL state (Q-4 で dept 削除、G-1 snake_case 整合)

```ts
const searchSchema = z.object({
  month: z.string().regex(/^\d{4}-\d{2}$/).optional().catch(undefined),
  mode: z.enum(["by_product", "by_department"]).optional().catch(undefined),  // G-1 で BIZ response も snake_case
  sortBy: z.enum(["name", "quantity", "amount", "prev_month_diff"]).optional().catch(undefined),
  sortDir: z.enum(["asc", "desc"]).optional().catch(undefined),
});
```

G-9 不正月 = zod undefined fallback + default 当月で吸収、UI 届く月は 1-12 範囲保証 (BIZ validate_month 前段保証)。

---

## §5 TanStack Query (Q-5 1 useQuery、失敗 4 状態)

(Round 2 で詳細確定、Round 3 で変更なし)

---

## §6 派生純関数 (F-10 + G-9 + H-3 UI 表示フォーマット)

**F-10 文字列操作実装**:
- `"2026-01".split("-")` → 数値分解 + month roll over
- ISO 形式再構築 (`prevMonth`/`nextMonth` 戻り値) のみ `padStart(2, "0")`
- 閏年判定: `(year % 4 === 0 && year % 100 !== 0) || year % 400 === 0`

**G-9 不正月 fallback**: 月番号 13/00 等は `"—"` 返却、throw new Error しない

**H-3 UI 表示フォーマット**: `formatMonthLabel("2026-01")` → **`"2026年1月"`** (zero-pad なし、モックアップ + UI-09a Intl 出力準拠)、`formatMonthLabel("2026-12")` → `"2026年12月"`

---

## §7 テスト構成 (Vitest 純関数 42-55 ケース、7 file + factory 2 種類 G-12、I-3 連動更新)

| file | ケース数 | 主内容 |
|---|---|---|
| compute-summary.test | 4-5 | 空/1件/複数/負数/大量 |
| compute-period-label.test | 6-8 | 1月/2月閏年 (2024)/2月非閏年 (2025)/12月/月番号 13 (「—」)/00 (「—」)/2000年/2100年 |
| compute-comparison.test | 8-10 | 当月>前月/<前月/同/0/null/+1000% (key 突合 + 0 ガード) + **Q-7: prev_amount = 0 → 比較不可 / prev_amount < 0 (Z004 返品超過、例 prev=-500) → 比較不可 / prev_amount < 0 + current_amount < 0 → 比較不可** |
| compute-composition.test | 5-7 | 空/100%/合計/端数/grand_total=0 ガード |
| pick-top-ranking.test | 5-7 | 空/5/10/15 (top10)/BIZ row_number 同順位なし前提 |
| sort-items.test | 6-8 | 空/4 列ソート/同値/null 末尾 + G-3 ranking バッジ追従 |
| format-month-label.test | 10-12 | 月またぎ/年またぎ/不正値/**H-3: UI 表示「2026年1月」「2026年12月」zero-pad なし** + UTC/JST 同結果担保 |

**G-12 factory 2 種類**:
- `makeMockMonthlyItem(overrides)`: DTO 5 field
- `makeMockProductRankingRow(overrides)` / `makeMockDeptCompositionRow(overrides)`: UI 派生 + `prev_month_diff?: number | null`

---

## §8 Verification (14 軸スクリプト + 14 項目手動)

### 8.1 スクリプト 14 軸 (Round 3 反映)
1. `cargo fmt --check` (src-tauri) / 2. `cargo clippy -- -D warnings` / 3. `cargo test --test architecture_test` (L1) / 4. `cargo test --test design_compliance_test` (L2) / 5. `cargo test` 全件 / 6. `cargo run --manifest-path src-tauri/Cargo.toml --bin generate_bindings` (G-11) / 7. `rg -n -e "MonthlySaleItem" -e "MonthlySalesReport" -e "SalesMode" -e "SalesReportType" -e "getMonthlySales" src/lib/bindings.ts` + `rg -n -e "by_product" -e "by_department" src/lib/bindings.ts` (G-1 snake_case + H-5 SalesReportType 確認) / 8. `npm run typecheck` (pretypecheck で `tsr generate` 自動) / 9. `npm run lint` / 10. **bindings trim** (user P2、worm 警戒下 npx 不使用) = `./node_modules/.bin/prettier --write --ignore-path /dev/null src/lib/bindings.ts` または `sed -i 's/[[:space:]]*$//' src/lib/bindings.ts` (F-5) / 11. `npm test` / 12. `./scripts/doc-consistency-check.sh` (設計書 21 項目) / 13. **`--target plan <path>`** = commit 1 前: `/home/kosei/.claude/plans/plans-recursive-allen.md` (home `.claude/`、**repo 内 `.claude/plans/` は存在しない方針** H-8) / commit 1 後: `docs/plans/2026-05-18-phase-2-ui-09b.md` / 14. `check-typedinvoke-count.sh` + `check-env-safety.sh`

### 8.2 手動 14 項目 (H-4 worm 判定 + H-9 IME 不要)

1-13. (Round 2 と同じ、Q-5 失敗 4 状態 + Q-4 部門列なし + G-3 1 位バッジ追従含む)
14. **H-4 worm 検証**: `gh api /advisories/GHSA-g7cv-rxg3-hmpx --jq '{state, withdrawn_at, published_at}'` 実行、**判定式 = `.withdrawn_at == null` なら警戒継続** (state が null/published/closed いずれでも未撤回扱い、archive plan `2026-05-17-vitest-install-safety.md` L128-140 jq + bash パターン引用) + `npm audit --audit-level=high` で baseline + 別 PC `npm ci --ignore-scripts && ls node_modules/@radix-ui/react-progress/dist/index.mjs` で transitive 展開 verify
- **F-11 Windows native 検証項目**: `<input type="month">` picker / Tooltip / Sonner / Progress アニメ + **H-9: IME 確認は本画面では不要 (日本語テキスト入力 field 0 件、`<input type="month">` + Select + Button + Tooltip のみ)**

### 8.3 LSP/Skills Policy: 27 file baseline → Write → URI 0 error/warning

---

## §9 Commit Split (7 commits、squash merge、Round 3 反映)

### Commit 1: `docs(ui-09b):` 11 file (F-12 4 分類 + H-2 path 明示)

(a) **新規 docs (2 file)**: plan 移送 + 関数設計新規 57-ui-monthly-sales.md
(b) **既存 docs 補強 (3 file)**: **`docs/FUNCTION_DESIGN.md` (root 直下、H-2)** 索引追加 (L42 UI-09a 「2 useQuery」隣に UI-09b 「1 useQuery + prev_month_comparison field」exposition 追加 + L111 索引 1 行追加 = 2 行更新、G-7) + `docs/architecture/ui-task-specs.md` §UI-09b 補強 + `docs/SCREEN_DESIGN.md` 月次節
(c) **Q-2 Sonner id drift (2 file)**: 関数設計 56-ui-daily-sales.md L232-233/L335-336 + archive plan 2026-05-17-phase-2-ui-09a.md 末尾 post-merge note 1 行 (F-14)
(d) **Q-3 TabsHeader docs drift (3 file)**: 関数設計 56-ui-daily-sales.md L43/L296 + `docs/architecture/ui-task-specs.md` L229 (F-7)
+ **罠 2 防御**: `rg -n -e 'monthly' -e '月次' -e 'export-daily-csv' -e 'MonthlySales' docs/ src/` 4 系統 grep 一括
+ **G-15**: 11 file を 1 commit で book、PR squash merge

### Commit 2: `feat(cmd):` specta 化 **6 点** (H-1 + H-5)
sales_service.rs `SalesMode` derive + `#[serde(rename_all = "snake_case")]` (H-1 response serialize のみ) + `MonthlySalesReport` derive + `MonthlySaleItem` derive + **`SalesReportType` derive + `#[serde(rename_all = "snake_case")]` (H-5)** + sales_cmd.rs `get_monthly_sales` specta + collect_commands + lib.rs doc + design_compliance_test SKIP_DOCS + bindings 再生成 (G-11) + bindings trim (F-5)
合計: **5 unique file** (specta 化対象 6 点を 5 file に集約: sales_service.rs 4 derive + sales_cmd.rs 1 specta + lib.rs collect + bindings.ts 再生成 + design_compliance_test SKIP_DOCS、I-1)

### Commit 3: `refactor(ui):` TabsHeader 移送
合計: 新規 1 + 更新 1 + 削除 1 = **3 file**

### Commit 4: `feat(ui):` 8-7 共通化 (G-4 + Q-2 + H-5)
`useExportFile.ts` 新規 (G-4 try/catch + downloadBlobFromBase64 private 移送 + **H-5: SalesReportType を bindings から import**) + `useExportDailySalesCsv.ts` wrapper 化 + query-keys
合計: 新規 1 + 更新 2 = **3 file**

### Commit 5: `feat(ui):` lib + types + hooks
合計: 新規 **10 file** (lib 8 含む factory G-12)

### Commit 6: `feat(ui):` components + page + route + nav + Progress (G-2 + G-5 + H-3)
- G-2 Progress wrapper (`import from "radix-ui"` 統合パッケージ)
- components 7 + MonthlySalesPage + routes/reports/monthly.tsx
- G-5 navigation.ts ui-09b ブロック anchor 文字列 (実機 L93 + L95)
- F-8 routeTree.gen.ts 自動再生成
合計: 新規 9 + 更新 1 = **10 file**

### Commit 7: `test(ui):` + `chore:` Vitest + Plans sync (G-6 + G-7 + G-14 + H-6 + H-7)
Vitest 7 file (G-12 factory 2 + G-3 + H-3 test) + pre-push hook 全 pass + LSP diag 0 + **Plans.md update**:
- L7 (Phase 行): PR #66 追加 + 「8-5」「8-7」削除
- L28 (Next Session Entry): 8-4 UI-06a 2 軸併記
- L31 (G-7): 「BIZ 単一 call の場合は 1 useQuery + 派生フィールド利用、複数 call の場合は 2 useQuery 部分障害許容を選択的に横展開」
- **L107 (H-7 + I-5)**: PR #65 メモ末尾 1 句追記「Sonner id は UI-09b で `export-daily-*` に rename (`-csv-` セグメント削除)、本 PR 当時の `export-daily-csv-success`/`export-daily-csv-error` は history reference」 **+ 同行 [x] 化 + commit hash 追記** (sequential edit で 1 行内吸収)
- L108-111: `[x]` 化 + commit hash 追記
- L155 (G-6): Backlog 文言更新 「PR #65 + #66 で 2 度発生、Rule-of-three 適用候補」
- **L156 (G-14)**: 「UI-09a 設計判断の将来精査 3 項目」 → 「UI-09a/b 設計判断の将来精査 5 項目」拡張統合 (Q-1 + Q-4 追加)
+ **H-6**: archive plan `2026-05-18-phase-2-ui-09b.md` 末尾に screen_mockups.html drift 3 件集約 note (3 行)
+ 罠 4 防御 (HEAD 表記分離)
合計: 新規 7 (Vitest) + 更新 2 (Plans.md + archive plan `2026-05-18-phase-2-ui-09b.md` H-6 note) = **9 file** (I-2 連動更新、commit 1 で plan 移送した先を commit 7 で末尾 note 追記 = 更新扱い)

### G-10 + H-10: 各 commit 単独 typecheck pass は本 PR では保証しない、**PR squash merge 前提で全体 typecheck pass**
- commit 3 単独で `<Link to="/reports/monthly">` 書くと routeTree 未配線で fail
- **H-10**: commit 5 (lib + types + hooks) は route 非依存だが、commit 6 を待たない単独 typecheck は routeTree 旧版で fail 可能、squash merge 後の最終 state のみ pass 担保
- 開発中の rebase/fixup で順序操作する場合は最終 commit pre-push 段階で全体 typecheck pass 確認

### 9.1 PR description (Round 3 反映)

```markdown
## Summary
- Phase 2 8-5 UI-09b 月次売上 (REQ-502 / SP-502-01〜06)
- specta 化 6 点 (関数 + SalesMode/MonthlySalesReport/MonthlySaleItem/SalesReportType 4 型 + lib.rs collect_commands、H-1 response only / H-5 SalesReportType 追加)
- 8-7 useExportFile 共通化 (G-4 try/catch + private 移送、H-5 SalesReportType bindings import) + UI-09a wrapper 化 + Sonner id 統一
- TabsHeader 共通化 + ラベル「日次/月次」 + router-driven
- Progress wrapper 手動配置 (G-2 radix-ui 統合パッケージ慣習)
- 1 useQuery (Q-5、UI-09a 2 useQuery 横展開は誤り判明)
- Q-4 部門情報削除 + Backlog / Q-1 「期間」カード + Backlog
- UI 表示「2026年1月」 zero-pad なし (H-3)、不正月「—」 (G-9)

## Codex Review 依頼
- H-1 SalesMode rename response only / H-2 docs/FUNCTION_DESIGN.md root / H-3 UI 表示 zero-pad / H-4 worm withdrawn_at 判定 / H-5 SalesReportType specta 化 / H-6 screen_mockups drift 集約 / G-1〜G-15 + F-1〜F-15 + Q-1〜Q-5
```

---

## §10 代替案 (Round 3 で追加、合計 26 個 I-4)

(Round 0/1/2 で 23 個 = 本 plan 初版で詳細列挙済、本セクションは Round 3 で追加された 3 個のみ列挙:)
- **SalesMode request も snake_case rename (H-1 却下、CMD 層 String 受け既存設計維持)**
- **`SalesReportType` frontend 独自定義 (H-5 B 案却下、二重管理 drift リスク、specta 一元化 A 案採用)**
- **UI 表示「2026年01月」zero-pad あり (H-3 却下、モックアップ + UI-09a Intl 出力 zero-pad なし「2026年5月17日」整合)**

合計 **26 個** (Round 0/1/2 = 23 個 + Round 3 = 3 個、I-4 連動明示)。Round 0/1/2 の 23 個は本 plan の Round 1 (移送前の) version で列挙済、最終 commit 1 で `docs/plans/2026-05-18-phase-2-ui-09b.md` 移送時に統合掲載。

---

## Self-Review (7 観点、Round 3 反映)

### 1. prerequisites
> §2.2 で specta 化対象 6 点 (H-5 で SalesReportType 追加)、H-1 で SalesMode rename は response only 確定 (sales_cmd.rs:54-65 実機検証)、H-2 で `docs/FUNCTION_DESIGN.md` root path 確定、H-3 で UI 表示「2026年1月」zero-pad なし確定。

memory `feedback-design-doc-tech-premise-verify-from-output.md` (Round 3 で H-1/H-2/H-4 実機確認 3 度実証)、`feedback-naming-must-match-reality.md` (Q-1/Q-3/Q-4 + H-3 4 度実証)、`feedback-recommend-with-explicit-basis.md` (実機 bash で裏取り)。

### 2. scripts
> §8.1 で H-5 SalesReportType 検証追加 (rg `by_product`/`by_department` + bindings 出力)、H-4 worm 判定式 `.withdrawn_at == null`、H-8 plan path home/repo 区別。

### 3. verification
> §8.2 14 項目で H-9 IME 不要明示 (日本語テキスト入力 0 件)。純関数 test **42-55 ケース** (I-3 連動更新)、特に format-month-label H-3 UI 表示「2026年1月」zero-pad なし test + sort-items G-3 ranking バッジ追従 test。

### 4. post-processing
> §9 commit 7 Plans.md update に H-7 L107 PR #65 メモ追記 + G-6 L155 + G-14 L156 拡張統合 + H-6 archive plan 末尾 screen_mockups.html drift 3 件 note。

### 5. constraints

> Mini Shai-Hulud worm 警戒下 install 凍結 (Plans.md MEMORY.md `feedback-npm-install-blocked-mini-shai-hulud-2026-05.md` 参照、2026-05-13 開始): H-4 判定式 `gh api /advisories/GHSA-g7cv-rxg3-hmpx --jq '.withdrawn_at == null'` で警戒継続判定、本 PR 着手 2026-05-19 時点で worm 警戒開始から 6 日経過、解除条件「7 日連続 clean + user 手動承認」に 1 日不足のため本 PR 内では警戒維持 + 新規パッケージ install 0 で完結させる。

Progress wrapper (G-2) は `@radix-ui/react-progress@1.1.7` node_modules 既導入の transitive dep として再利用、新規 install 0。cwd は `cargo run --manifest-path src-tauri/Cargo.toml --bin generate_bindings` で明示 (G-11、Plans.md README L86 fix 同型)。bindings.ts trailing whitespace は `.prettierignore` で除外されているため `npx prettier --write --ignore-path /dev/null src/lib/bindings.ts` または `sed -i 's/[[:space:]]*$//' src/lib/bindings.ts` で trim (F-5、`npm run format` 単独では動かない)。memory `feedback-radix-tooltip-aria-disabled.md` 3 層パターン (印刷 button: aria-disabled + onClick preventDefault + cursor-not-allowed opacity-60)、memory `feedback-non-it-user-feature-minimal-build.md` で月選択カスタム化 / chart library / 営業日数 BIZ 拡張 / 部門情報 BIZ 拡張 / Sonner id 後方互換 wrapper / bindings trim 自動化 の 6 つ scope 抑制 (Q-1 + Q-2 + Q-4 + F-15 + G-6 で実証)、memory `feedback-codex-drift-fix-grep-all-locations.md` 準拠で `rg -n -e 'monthly' -e '月次' -e 'export-daily-csv' -e 'MonthlySales' docs/ src/` の 4 系統 grep を commit 1 で一括処理 (罠 2 防御)。memory `feedback-pr-merge-gate-scope-discipline.md` 警戒下で UI-09b + 8-7 共通化 + TabsHeader 共通化 + Progress wrapper 配置 + SalesReportType specta 化 の 5 機能同 PR scope を §8 verification 14+14 + commit 単一責務分離 + Plan rally 5 round (累積 50 件発見) で抑える。

### 6. commit split
> §9 7 commit + G-10/H-10 squash 前提明示 (commit 3 単独 fail + commit 5 単独 fail 注記)、F-12 4 分類 + G-5 anchor 文字列 + G-15 commit 1 squash + H-5 specta 化 6 点で commit 2 範囲拡張。

### 7. rally
> **Round 0** = 初版 + 訂正 4 / **Round 1** = 15 件 (F-1〜F-15) / **Round 2** = 15 件 (G-1〜G-15) / **Round 3** = 10 件 (H-1〜H-10) / **Round 4** = 5 件 (I-1〜I-5) / **Round 5** = 5 件 (J-1〜J-5) / **Round 6 (本版)** = **user human-driven critique 3 件**: P1 critical (Q-6 A 案 = `export_sales_csv(report_type: String)` → `(SalesReportType)` CMD signature 変更で fully specta 化、UI-09a wrapper 化で吸収) + P2 important (Q-7 = prev_amount <= 0 比較不可「—」、Z004 返品月の色分け逆転回避) + P2 important (npx prettier worm 経路回避 = local + sed 一本化) 全反映。verdict = **converged**。

memory `feedback-plan-mode-recursive-refinement.md` 「4 段ラリー = 35 件発見」と整合、本 PR は **Plan rally machine-driven 5 round (Round 1-5、累積 50 件) + user human-driven 1 round (Round 6、3 件) = 累積 53 件発見 + 全反映**。memory `feedback-claude-self-bias-blind-spot.md` 「Claude 自主判断は信頼しない、独立 context の subagent で自己 bias 発見」が Round 1-5 で 50 件発見の実証、user critique で更に 3 件 = bias 残存の最終証拠 (specta 経路 / 業務仕様の負数ケース / worm 警戒下のコマンド経路選択 = いずれもドメイン知識 + 業務文脈の必要な判断軸で、machine-driven critique では発見困難)。Round 6 後は **converged 判定**、ExitPlanMode 進行。残 minor drift (もしあれば) は実装段階の commit 1 `docs/plans/` 移送時に統合修正。

---

## Critical Files (Top 5、Round 3 反映)

1. `docs/function-design/57-ui-monthly-sales.md` (新規)
2. `src-tauri/src/biz/sales_service.rs` (specta::Type + rename_all H-1 + SalesReportType H-5) + `cmd/sales_cmd.rs` + `lib.rs`
3. **`docs/FUNCTION_DESIGN.md` (H-2 root 直下)** + `src/components/sales/TabsHeader.tsx` (Q-3) + `src/components/ui/progress.tsx` (G-2)
4. `src/lib/hooks/useExportFile.ts` (G-4 + H-5 SalesReportType import) + `src/features/daily-sales/hooks/useExportDailySalesCsv.ts` (wrapper)
5. `src/features/monthly-sales/hooks/useMonthlySalesReport.ts` (Q-5) + `lib/format-month-label.ts` (F-10 + H-3) + `lib/compute-period-label.ts` (G-9)

---

## 規模感

- commit 数 7
- 新規 20 (本体、§1.4) + Vitest 7 (§7) + 移送 1 + 更新 5 + 削除 1 (TabsHeader 旧版) = **34 file 実体** (§8.3 LSP baseline 27 file = 本体 20 + Vitest 7、J-5 連動明示)
- 行数 約 2800-3300 行
- **Plan rally Round 5 で converged (累積 50 件発見、Round 1=15 / 2=15 / 3=10 / 4=5 / 5=5 で減少傾向継続、Round 6 省略 = self-reference 循環ループ回避、ExitPlanMode 進行)** (J-2 連動更新)
- Codex review 2-3 round で merge 想定
- 実装 14-16h

---

## User Q-1〜Q-5 + Plan rally F-1〜F-15 + G-1〜G-15 + H-1〜H-10 + I-1〜I-5 + J-1〜J-5 全反映済 (累積 50 件、J-3 連動更新)

- ✅ Q-1〜Q-5 (Round 0/1 確定)
- ✅ F-1〜F-15 (Round 1 反映)
- ✅ G-1〜G-15 (Round 2 反映、実機裏取り 2)
- ✅ H-1〜H-10 (Round 3 反映、実機裏取り 3)
- ✅ I-1〜I-5 (Round 4 反映、数値整合 drift)
- ✅ J-1〜J-5 (Round 5 反映、plan top/bottom メタ情報 self-reference drift、本版)

**Plan rally Round 6 省略 = self-reference 循環ループ回避** (memory `feedback-self-trace-expression-breaks-sync-loop.md`)。Round 5 反映で plan の主要メタ情報すべて Round 5 状態で停止、これ以降の Round critique は同型の self-reference drift を再発生させるリスク (Round N 反映済表記 → 次 Round で陳腐化 → 反映 → 次 Round で陳腐化の循環)。本 plan は本 Round 5 状態で **converged 判定**、ExitPlanMode 試行可能。残 minor drift は実装段階で plan を `docs/plans/` 移送する際の relocation commit (commit 1) で同時に統合修正。

---

## post-merge note: screen_mockups.html drift 3 件 (H-6)

UI-09b 実装で `docs/screen_mockups.html` L398-404 の以下 3 件が drift。関数設計 [57-ui-monthly-sales.md](../../function-design/57-ui-monthly-sales.md) と [SCREEN_DESIGN.md](../../SCREEN_DESIGN.md) 月次節を**正**、mockup は historical reference として**不変**とする (Q-1 / Q-3 / Q-4 で確定済の業務判断):

1. **Q-1 営業日数**: mockup に「営業日数」「日平均」表示あり → BIZ-05 `MonthlySalesReport` DTO に該当 field なし、UI で算出できない (祝日マスタ未導入) → 「期間: YYYY/MM/DD-MM/DD」固定文言で代替
2. **Q-3 tab ラベル**: mockup に「商品別 / 月次集計」表示 → 売上レポート 2 画面 (日次 / 月次) の TabsHeader を共通化、ラベル「日次 / 月次」に統一
3. **Q-4 部門フィルタ**: mockup に部門フィルタ Select + 商品ランキング「部門」列あり → `MonthlySaleItem` DTO に部門情報なし、UI で派生不可能 → 部門フィルタ非対応、ランキングテーブル「部門」列なし

将来 BIZ-05 拡張で営業日数 / 部門情報を追加する場合は別 PR (Plans.md Backlog L156 (4)(5) 参照)。
