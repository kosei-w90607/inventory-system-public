# Phase 2 UI-00 commit 4 → 5 → PR open 実行計画

## Context

UI-00 ホーム画面実装の commit 1〜3 完了済 (`feat/ui-00-home` branch、commit `c619c06` / `2c1ac37` / `41b14fa` / `5a5f2fa`)。次セッションは commit 4 (hooks + 純関数 + presentational components 11 ファイル) → commit 5 (HomePage mount + route 接続) → 統合動作確認 → PR open を実行する。`docs/DEV_SETUP_CHECKLIST.md` §13.1 の Phase 2 着手前 gate「Windows native ビルド移行」が未済で、UI-00 本体の入力フォーム不在を踏まえつつも PR #50 の setTitle WebKitGTK 固有事象再発防止のため PR open 前に Windows `tauri dev` 通過を要求する。

着手前ブロッカーは shadcn `Alert` 未導入のみ。関数設計書 `docs/function-design/53-ui-home.md` と既存コード (query-keys.ts / bindings.ts / invoke ラッパ / navigation.ts / RootLayout) の整合度は 95%。**Step 0 (hook 改修) は前セッションで commit `4ab7017` 済、本セッションは Step 1 から開始**（P3-A 反映）。

## 主要判断 (確定)

| 判断項目 | 採用 | 根拠 |
|---|---|---|
| Windows native 移行 | commit 4 と並行構築 (ハイブリッド案) | UI-00 は入力フォーム無しだが §13.1 gate と PR #50 setTitle 固有事象再発防止のため PR open 前に Windows `tauri dev` 通過を要求。並行構築でコンテキストスイッチを最小化 |
| commit 4 を分割しない | 1 commit 統合 | `memory/ui-design-impl-bundled-pr.md` + `frontend-function-design-granularity.md`。pre-push hook の中間状態を回避 |
| Alert 導入を独立 commit | commit 3.5 として分離 | shadcn add の generated diff が大きく review 容易性で本体 commit 4 と分ける |
| inventory-code-review skill | commit 5 後 PR open 直前に 1 回 | commit 4 単独だと使用側未配線で誤検知。機械チェック先行 (`memory/codex-review-workflow.md`) |
| Codex review | PR open 直後に依頼 | `.claude/rules/review-workflow.md` 連携手順 1 |
| Plans.md 同期 | PR merge gate 直前に boundary milestone 1 commit | `memory/feedback-plans-sync-commit-milestone-only.md` |

## 実行制約 (全 step 共通)

- **merge / force push / shared state 変更は user 明示承認が必要**: Step 11 squash merge は user 操作。Claude が `gh pr merge` を勝手に実行しない
- **Codex review 依頼は user の操作**: Step 8 PR open 後、Codex 依頼テキストを Claude が出力 → user が ChatGPT Plus Codex app で実行 → 結果を Claude にフィード
- **LSP/Skills Policy hook**: Step 3 / Step 4 の Write/Edit 前に必須:
  1. LSP `getDiagnostics` で baseline 取得
  2. `.claude/state/skills-decision.json` を更新 (該当 skills とその採否を明記)
  3. その後 Write/Edit 実行 → URI 指定 `getDiagnostics` で差分検証
  4. (memory `feedback-lsp-skills-policy-hook.md` 参照、docs 編集は適用外)
- **commit 中間 push の方針**: commit 4 完了直後はバックアップ push しない (pre-push hook が typedinvoke / env-safety / doc-consistency 全部走る、中間状態で fail するリスク)。commit 5 まで通して Step 8 で 1 回 push

## ステップ依存関係

```
[完了済] Step 0 (hook 改修, commit 4ab7017)
            ↓
            ├─ Step 1 (Windows native 環境構築) ──────┐ (並行、Step 6 着手前までに完了)
            │                                          │
            └─ Step 2 (Alert 追加)                     │
                ↓                                      │
                Step 3 (commit 4 実装)                 │
                ↓                                      │
                Step 4 (commit 5 実装)                 │
                ↓                                      │
                Step 5 (自動検証フルバッテリー)        │
                ↓                                      │
                Step 6 (Windows 手動検証) ←────────────┘
                ↓
                Step 7 (セルフレビュー)
                ↓
                Step 8 (PR open)
                ↓
                Step 9 (Codex Round 1 対応)
                ↓
                Step 10 (Plans.md 同期)
                ↓
                Step 11 (merge + post-processing)
```

実行ガイド（P3-B 反映）:
- Step 1 は Step 2-5 と並行可能 (作業領域が独立、WSL2 と Windows は別環境)
- Step 1 の deadline は **Step 6 着手前**
- Step 1 が deadline までに完了しない場合は失敗時 fallback (Phase 2 8-2 へ繰延) を発動

## ステップ

### Step 0 — Hook 改修 (セルフレビュー強制 + 整合チェック登録) [完了済 / commit `4ab7017`]

**状態**（P3-A 反映）: 2026-05-09 完了。本 plan セッションで実行不要 (Step 1 から開始)。以下は履歴記録として残す。

**目的**: ExitPlanMode 前のセルフレビュー漏れを構造的に止める。CLAUDE.md 記載と settings.json 実態の乖離を修復 (`check-plan-on-exit.sh` が settings.json に未登録だった)。今回 user が「忘れちゃうのか、どうすりゃいい」と指摘した根本原因を修正。

**設計判断**:
- check-plan-on-exit.sh 拡張案を採用 (1 hook で整合 + セルフレビュー両方検証、責務集約)
- 検証ロジック: plan 本文に `## Self-Review` または `## セルフレビュー` セクション (or 7 観点キーワード) が存在しない場合 deny
- 適用除外: plan 本文に「Self-Review: 適用除外 (1-3 ファイル軽微タスク)」が記載されていれば通す (memory `plan-self-review-before-implementation.md` 適用除外条項)

**作業**:
- `.claude/hooks/check-plan-on-exit.sh` を拡張
  - 既存ロジック: `scripts/doc-consistency-check.sh --target plan` 実行
  - 追加: 直近更新の plan ファイルを取得 (`suggest-subagent-for-plan.sh` の探索ロジックを参考) → `## Self-Review` 見出し検証 (rg 利用)
  - セクションも適用除外マーカーも無ければ deny + additionalContext に 7 観点チェックリスト出力
  - 既存 doc-consistency-check と AND 条件 (両方通過で allow)
- `.claude/settings.json` の `PreToolUse.ExitPlanMode.hooks` に `check-plan-on-exit.sh` を追加 (既存 `suggest-subagent-for-plan.sh` と並列、timeout 10)
- 動作確認: dummy plan を 2 種作成 (Self-Review あり / なし) → 模擬 hook 入力で deny / allow が分かれることを確認
- 既存 plan (本 plan + `docs/plans/phase-2-ui-00.md`) に `## Self-Review` セクションを追記して既存 plan も準拠状態にする
- コミット `chore(hooks): enforce plan self-review at ExitPlanMode`
- **本 plan が hook で block される問題回避**: 本 plan ファイルにも `## Self-Review` セクションを追加 (本ファイル末尾参照)、Step 0 完了後に hook が working

**完了判定**:
- shellcheck `.claude/hooks/check-plan-on-exit.sh` exit 0
- dummy plan (Self-Review なし) で hook が permissionDecision: deny を返す (jq で確認)
- dummy plan (Self-Review あり) で permissionDecision: allow を返す
- settings.json: `PreToolUse.ExitPlanMode.hooks` に 2 hook 登録 (suggest-subagent + check-plan-on-exit)
- CLAUDE.md の記述「整合エラーでブロック」と settings.json が一致

**失敗時**: hook deny ループで作業止まったら settings.json の `check-plan-on-exit.sh` 登録を一時コメントアウトして fallback (人手レビューで担保)。原因特定後に再有効化。

### Step 1 — Windows native 環境構築 (Step 2-5 と並行、Step 6 着手前 deadline)

**目的**: §13.1 gate を解消、Step 6 の手動検証 13-18 を Windows で実行可能にする。

**作業**:
- Windows 11 Home 側に Rust 1.83+ msvc target / Node 20 LTS / WebView2 Runtime 確認 / Microsoft C++ Build Tools (Desktop development with C++) 導入
- Git SSH 設定 (WSL2 と同じキーをコピー or 新規生成)
- `git worktree add` または `git clone` で `feat/ui-00-home` を Windows 側に同期 (推奨: git push/pull、`\\wsl$\` 直接アクセスは速度・改行コード問題で避ける)
- `git config core.autocrlf false` (project local) 設定

**並行運用ガイド**（P3-B 反映）:
- Step 2 (Alert 追加) / Step 3 (commit 4 実装) を WSL2 で進めながら、空き時間に Windows 側で本 Step を進める
- Windows 側 build 環境の installer 系 (Microsoft C++ Build Tools) は時間がかかるため、Step 2 着手と同時に並行起動推奨
- Step 6 着手時点で本 Step が未完了なら、失敗時 fallback 発動 (Phase 2 8-2 へ繰延)

**Claude サポート範囲 / user 操作範囲**（P3-G 反映）:

WSL2 sandbox 制約により Claude は Windows 側で実コマンド実行不可。役割分担を明示:

| 項目 | Claude サポート | user 操作 |
|---|---|---|
| Rust 1.83+ msvc target 導入 | rustup-init.exe 入手 URL + `rustup target add x86_64-pc-windows-msvc` コマンド提示 | ダウンロード + 実行 + PATH 確認 |
| Node 20 LTS 導入 | nvm-windows or 公式 installer の選択判断ガイド + バージョン確認コマンド | installer 実行 + バージョン確認 |
| Microsoft C++ Build Tools | 必要 component 列挙 (Desktop development with C++ ワークロード) + winget command 提示 | Visual Studio Installer 起動 + workload 選択 + install |
| WebView2 Runtime | Windows 11 同梱確認方法 (`Get-AppxPackage` PowerShell) | コマンド実行 + 結果報告 |
| Git SSH 設定 | WSL2 ~/.ssh/ から Windows ~\.ssh\ へのコピー手順 + ssh-agent 設定 | ファイルコピー + 権限設定 |
| feat ブランチ同期 | `git worktree add` vs `git clone` の選択判断 + コマンド提示 | コマンド実行 |
| `core.autocrlf false` | 設定根拠 (改行コード問題回避) + コマンド提示 | コマンド実行 |
| `cargo build` / `npm run tauri dev` 動作確認 | エラー出たら diagnose + 対処コマンド提示 | コマンド実行 + ログ報告 |
| 失敗時 fallback 判定 | 「Phase 2 8-2 へ繰延 vs WSL2 完結継続」の損益判断 | 最終判断 |

サポート利用フロー:
1. user が Step 1 任意項目に着手 → Claude に「次何やる?」と聞く
2. Claude が当該項目のコマンド + 期待結果 + 失敗時の典型 error pattern を提示
3. user が実行 → 結果 (成功 / error log) を Claude に報告
4. Claude が次手順 or トラブル対応を返す

これにより Step 1 全体を user-Claude のチャット往復で完走できる。一方的に「自分で調べて」と投げる構図にはしない。

**完了判定**: Windows で `cargo build` 通過 + `npm run tauri dev` で現状ホーム (search_products demo) が描画される。

**失敗時**: ハイブリッド案を保留、commit 4-5 を WSL2 完結 + IME / setTitle 検証は Phase 2 8-2 (UI-07) へ繰延、Plans.md backlog に明記。

### Step 2 — shadcn Alert 追加 (commit 3.5)

**目的**: `docs/function-design/53-ui-home.md` §53.5「カード内エラー = `<Alert variant="destructive">`」依存を満たす。

**作業**:
- `npx shadcn@latest add alert` → `src/components/ui/alert.tsx` 生成
- `git commit -m "chore(ui): add shadcn Alert component"`

**完了判定**: `npm run typecheck` / `lint` / `format:check` 緑、`./scripts/check-typedinvoke-count.sh` baseline 0 維持。

**失敗時**: shadcn registry 障害なら手動コピー、`rg "from \"@/components/ui/alert\""` で参照箇所確認。

**Step 6 事前確認の前倒し**（P3-C 反映）:

Step 6 で必要になる `VITE_MOCK_MODE` 環境変数の実装有無を **Step 3 着手前** に確認する:

- `rg "VITE_MOCK_MODE" src/` で実装箇所を grep
- `src/lib/env.ts` の現状コードと PR #52 commit 3-5 を照らす
- 実装済なら Step 3 で hook 内に MOCK 分岐を組み込み (commit 4 内)
- 未実装なら Step 6 fallback (DB 一時 rename) を採用、Step 3 hook では分岐不要

事前確認結果は Step 3 着手判断 + Step 6 手段確定の両方に影響するため、commit 3.5 完了直後の本タイミングで実施。

### Step 3 — commit 4 実装 (hooks + 純関数 + presentational components)

**目的**: §53.1〜§53.6 を 1 commit に圧縮実装。

**事前手順 (LSP/Skills Policy hook 対応、Write 前必須)**:
1. `getDiagnostics` で baseline 取得 (現状の WorkspaceDiagnostics)
2. `.claude/state/skills-decision.json` を更新: `frontend-design` / `react-19` / `tailwind-4` / `typescript` / `inventory-code-review` の採否を明記
3. Write 実行 → 各ファイルに URI 指定 `getDiagnostics` で差分検証 (`memory/feedback-lsp-skills-policy-hook.md`)

**作業**: `src/features/home/` 配下 11 ファイル一括作成
- `types.ts` (HomeSummaryState 等)
- `hooks/useHomeSummary.ts` — 4 useQuery 独立 (`unwrapResult(commands.*)` + `queryKeys.*` 経由)、staleTime 個別 (`dailySales` 5min / `pluDirty` 30sec / 他 60sec、D-5)、enabled は `dailySales` のみ `yesterday !== null`、派生値計算 (outOfStockCount / lowStockCount / pluDirtyCount / lastImportSettlementDate / needsImportWarning)
- `hooks/useYesterdayDate.ts` — `toLocaleDateString("sv-SE")` で JST 昨日 YYYY-MM-DD、24:00 またぎは queryKey 変化で再 fetch
- `lib/count-stock-status.ts` — 純関数 (`stock_quantity <= 0` / `> 0` で分類、D-1)
- `components/PluNotificationBar.tsx` — `pluDirtyCount >= 1 && !isLoading` で表示、UI-08 遷移は pending disabled
- `components/SummaryCards.tsx` — 3 カード束ね
- `components/SummaryCard.tsx` — 3 状態 (Skeleton / Alert + 再試行 / data)
- `components/QuickActionGrid.tsx` — 2×2 全 pending (CSV取込 / 日次売上 / 在庫照会 / 商品管理 = navigation.ts SSOT で全 pending、Phase 2 後続 PR で順次 active 化予定、Codex PR #56 P2-1 反映)
- `components/InventoryActionGrid.tsx` — 2×2 全 pending
- `components/MiscActionRow.tsx` — 3 ボタン全 pending
- `components/ActionButton.tsx` — props は `navItemId: NavItem["id"]` のみ、内部で navigation.ts 参照して status/to/label 取得 (SSOT、prop drilling 回避)、pending 時は `<Tooltip content="後続フェーズで着手予定">` + aria-disabled + onClick preventDefault + cursor-not-allowed (HTML disabled は不採用、Codex PR #56 P2-1 + commit `00aae19` 反映)

**完了判定**: LSP diagnostics 空 / `npm run typecheck` / `lint` / `format:check` 通過、コミット `feat(home): hooks + 純関数 + presentational components`。

**失敗時**: 型エラーは bindings.ts L177-179 の `& Product` intersection で snake_case fields 提供を確認。Hook mock は `VITE_MOCK_MODE` (PR #52 7-10) 流用。

### Step 4 — commit 5 実装 (HomePage mount + route 接続)

**目的**: search_products demo を撤去し UI-00 を `/` route で公開。

**事前手順**: Step 3 同様、LSP `getDiagnostics` baseline + `skills-decision.json` 更新を Write 前に実行。

**作業**:
- `src/features/home/HomePage.tsx` 新規 — ヘッダ「在庫管理システム」+ 今日の日付 + `<PluNotificationBar/>` + `<SummaryCards/>` + `<QuickActionGrid/>` + `<InventoryActionGrid/>` + `<MiscActionRow/>`、`useHomeSummary()` 呼び出し
- `src/routes/index.tsx` 差し替え — 現状 93 行 (search_products demo) を 3 行 (`createFileRoute` + `<HomePage />` mount) に圧縮
- `docs/function-design/53-ui-home.md` 更新履歴に「実装完了 (PR #N)」追記。`docs/FUNCTION_DESIGN.md` 対象モジュール一覧の UI-00 行も「実装完了」記載へ更新
- コミット `feat(home): HomePage + route 接続`

**完了判定**: `npm run build` 通過 / WSL2 `npm run tauri dev` で 4 カード Skeleton → data 遷移 / 大ボタン active route 遷移 / pending route disabled + Tooltip。

**失敗時**: TanStack Router codegen 古ければ dev server 再起動、route 競合は `src/routes/__root.tsx` 確認。

### Step 5 — 自動検証フルバッテリー

**目的**: PR 提出前 §検証チェックリスト 1-12 全項目通過。

**作業** (グループ A, B 内は順次、グループ間は並列可):
- グループ A (Rust): `cargo fmt --check` → `cargo clippy --all-targets --all-features -- -D warnings` → `cargo test` (561 本) → `cargo test --test architecture_test` → `cargo test --test design_compliance_test` → `cargo run --bin generate_bindings && git diff --exit-code src/lib/bindings.ts`
- グループ B (Frontend): `npm run typecheck` → `npm run lint` → `npm run format:check` → `npm run build`
- グループ C (Scripts、上記独立): `./scripts/doc-consistency-check.sh` / `./scripts/check-typedinvoke-count.sh` (baseline 0)

**完了判定**: 全 exit 0、bindings.ts drift なし、test 561 本維持。

**失敗時**: design_compliance_test fail なら関数名を `commands.*` 実体と再突合 (`memory/feedback-codex-drift-fix-grep-all-locations.md`、rg で全箇所確認)。

### Step 6 — Windows native ビルド + 手動検証

**目的**: §検証 13-18 を Windows で実行 (§1.4 Tauri 2 IME 制約 + PR #50 setTitle WebKitGTK 固有事象を Windows で否定)。

**事前確認**（Step 2 末尾で実施済、P3-C 反映）: `VITE_MOCK_MODE` の現状実装は PR #52 7-10 で env 設計のみ導入。実際の mock 切替コードが未実装の可能性 → Step 2 末尾の「Step 6 事前確認の前倒し」で `src/lib/env.ts` と features/home の hook の MOCK 分岐有無を rg で確認済。確認結果に応じて本 Step の手段を確定:

- 実装済の場合: `VITE_MOCK_MODE=error` 等で 4 項目のエラー強制テスト
- 未実装の場合: fallback として「DB の該当テーブル一時 rename → 再 query → 復旧」で代替

**作業**: Windows 側で `git pull origin feat/ui-00-home && npm install && npm run tauri dev`。手動確認:
1. Window title「在庫管理システム」日本語 render
2. 4 カード Skeleton → data
3. カード単位エラー強制 (`VITE_MOCK_MODE` or DB 一時退避) → 「取得失敗 + 再試行」
4. PLU bar 条件表示 (mock 切替で `pluDirtyCount=0/1+`) と UI-08 遷移 disabled
5. 大ボタン active route 遷移 / pending disabled + Tooltip
6. 24:00 またぎ (システム時計を翌日へ進めて再フォーカス → queryKey 変化で再 fetch)

**完了判定**: 6 項目 PASS、コンソールエラー 0、setTitle 日本語破損なし。

**失敗時**: Windows でも setTitle 固有事象が再現したら設計書 §53.5 にログ + Plans.md Backlog 記載、PR は draft で起こして相談。

### Step 7 — `inventory-code-review` skill セルフレビュー

**目的**: D-1〜D-10 整合 / 部分障害許容 / queryKey 規約 / 日付責務 を P1/P2/P3 ゼロまで潰す。

**作業**: `inventory-code-review` skill 起動 → `git diff main...HEAD` 全体に skill `references/review-checklist.md` 適用。

**完了判定**: P1/P2 ゼロ、P3 は same-PR 軽量対応 or backlog 化判定。

**失敗時**: P1 検出ならコミット差し戻し (commit 4 or 5 に追加コミット)、`memory/codex-non-blocker-incorporation.md` 同 PR 3 round 以内ルール適用。

### Step 8 — PR open

**目的**: Codex review に渡せる状態でリモート公開。

**事前判定 (rebase 要否)**:
- `git fetch origin main && git log --oneline HEAD..origin/main` で main の進行を確認
- main に新 commit があれば `git rebase origin/main` (force push なし、PR 未 open のため安全)
- main 同期済なら rebase 不要

**作業**:
- `git push -u origin feat/ui-00-home`
  - pre-push hook 4 ステップが全部走る:
    1. `cargo fmt --check` / `cargo clippy -- -D warnings` / `cargo test`
    2. `./scripts/doc-consistency-check.sh` (19 項目)
    3. `./scripts/check-typedinvoke-count.sh` (baseline 0)
    4. `./scripts/check-env-safety.sh` (.env / `src/lib/env.ts` 安全性)
- `gh pr create` で起票
- description に差分規模 + 重点レビュー観点 (specta 化引数 camelCase / 部分障害許容 / queryKey 規約 / 日付責務 / pending UX) を `docs/plans/phase-2-ui-00.md` §コミット分割 表のとおり明記
- Plans.md は **この時点では commit しない** (boundary 維持)
- Codex 依頼テキストを生成して **user に出力**。Codex app での実行は user 操作

**完了判定**: PR URL 取得、CI 全 green、Codex 依頼テキストをユーザーに出力。

**失敗時**: pre-push ① fail なら Step 5 グループ A へ、② fail なら DOC_STYLE_GUIDE 違反 / 設計書整合切れの修正、③ fail なら typedInvoke 件数増加箇所 (`memory/feedback-baseline-monotonic-ci-both-directions.md` で減少も fail) を rg、④ fail なら `src/lib/env.ts` の secret 露出を確認。CI fail (drift) なら bindings.ts 再生成漏れを疑い Step 5 再実行。

### Step 9 — Codex Round 1 対応 [完了 / Round 1-3 全 close, HEAD `2f1aa26`]

**目的**: ChatGPT Plus Codex app の指摘を P1 → P2 → P3 順に潰しマージ可状態へ。

**作業**: 指摘リスト受領 → 指摘ごとに patch コミット (`fix(home):` / `refactor(home):`)、3 round 以内に収束 (`memory/codex-non-blocker-incorporation.md`)。drift 系指摘は repo 全体 grep で全箇所一括修正 (`memory/feedback-codex-drift-fix-grep-all-locations.md`)。

**完了判定**: 全件対応、`gh pr checks` 全 green、Codex 再レビューで blocker 0。

**実施結果 (2026-05-09)**:
- **Round 1** (`00aae19` レビュー): P1: 0 / P2: 2 (P2-1 Tooltip wording "Phase 3" 誤り → 7 箇所 generic 化「後続フェーズで着手予定」+ P2-2 hook `|| echo 0` 整数比較バグ → `|| true); VAR=${VAR:-0}` パターン 5 箇所修正) / P3: 2 (P3-1 plan-rally.md `fd` → `find -name '*.md' -mmin -N` + P3-2 SummaryCards.tsx per-card loadingSkeleton 追加) → `e133175` で同 PR fix + CI Run #218 全 green
- **Round 2** (`e133175` レビュー): P1: 0 / P2: 1 (P3-1 fix で紛れ込んだ新規 bug = `find ... | xargs ls -t | head -1` の no-match 時 GNU xargs cwd fallback、`xargs -r` 追加で抑止) / P3: 0 → `2f1aa26` で同 PR fix + CI Run #220 全 green。Round 1 fix 4 件全 close 確認済
- **Round 3** (`2f1aa26` レビュー): P1: 0 / P2: 0 / P3: 0 = **Squash merge ready** 認定。Round 2 P2 fix close 確認 (`xargs -r` 動作再現済 + 同パターン grep 残存なし)

**失敗時**: 4 round 目突入なら scope creep 判断 → Plans.md backlog に切り出し PR を merge 優先。

### Step 10 — Plans.md 同期コミット (boundary milestone)

**目的**: `memory/feedback-plans-sync-commit-milestone-only.md` 「マージ前 boundary milestone 1 回」遵守。

**作業**:
- Plans.md Progress Tracker §第8段階 8-1 を `[x]` に更新
- Active Tasks の PR entry を完了記録に更新
- 8-6 の Q-3 採用結果は PR-A 適用済の確認のみ
- Plans.md Backlog L142「specta 化対象 commands 段階化リスト」を実測値で更新（P3-D 反映）:
  - 旧: `get_daily_sales` / `search_products` 既済 / `list_plu_dirty` / `get_low_stock_count` 等
  - 新: `get_daily_sales` ✓ / `list_low_stock` ✓ (D-1 採用で `get_low_stock_count` ではない) / `list_plu_dirty` ✓ / `list_csv_imports` ✓ (前日未取込み警告 D-8 用に追加)
- future-tense / progress-tense 系 keyword を rg で一括 grep 全置換 (`memory/feedback-status-sync-pr-keyword-grep-comprehensive.md`)
- コミット `docs: Plans.md sync — UI-00 PR #N merge ready` → push

**完了判定**: CI 緑、reviewer approve 後の merge 可能状態。

**失敗時**: Plans.md 競合は main 取り込み (rebase) で解消、push 再試行。

### Step 11 — PR merge → post-processing

**目的**: archive 移動 + 8-6 別 PR 着手準備 + memory 軽量監査。

**作業**:
- **squash merge は user 操作** (Claude は実行しない、user 承認後 `gh pr merge --squash` 提示)
- merge 後に Claude が以下を実行:
  - `docs/plans/phase-2-ui-00.md` を `docs/archive/plans/2026-05-XX-phase-2-ui-00.md` に rename
  - `docs/plans/2026-05-08-phase-2-ui-00-commit-0-revised.md` も同 archive へ移動
  - **`docs/plans/2026-05-09-phase-2-ui-00-commit-4-5.md` (本プラン) も同 archive へ移動**（P1-A 反映）
  - **3 ファイルすべて**で相対パス `../` → `../../` 全変換 (`memory/feedback-archive-relative-path-conversion.md`、PR #49 事故同種)
  - DEV_SETUP_CHECKLIST.md §13.1 の Windows native gate を `[x]` に
  - Plans.md Backlog の Phase 2 8-6 を「直近着手」に格上げ
  - **push 先**（P3-E 反映）: main 直接 commit。private repo + 個人プランで branch protection rule 制約なしのため許容 (Plans.md Backlog 「CI frontend job 名と branch protection rule 整合性確認」項参照)。コミット message: `chore: archive UI-00 plans + post-merge sync (PR #N)`
- **memory 軽量監査**:
  - 本 PR で発生した feedback / 判断軸 (Windows native ハイブリッド案 / セルフレビュー漏れ等) が memory/ に反映済か確認
  - 既存 memory のうち UI-00 関連 (`feedback-archive-relative-path-conversion.md` / `feedback-codex-drift-fix-grep-all-locations.md` / `frontend-function-design-granularity.md` / `ui-design-impl-bundled-pr.md`) が今回のサイクルで活かされたか追記検討
  - sentinel 更新: `touch /home/kosei/.claude/projects/-home-kosei-inventory-system/memory/.last_audit`

**完了判定**: `./scripts/doc-consistency-check.sh` 通過、`rg "docs/plans/phase-2-ui-00.md"` 0 件 (移動先のみ)、archive 内 link 切れなし、`.last_audit` mtime 更新済。

**失敗時**: link 切れは `rg -n "\.\./" docs/archive/plans/2026-05-XX-phase-2-ui-00.md` で残存箇所 grep して再変換。

## Critical Files

- `docs/plans/phase-2-ui-00.md` — commit 1-5 内訳 + Acceptance Criteria の出典
- `docs/function-design/53-ui-home.md` — §53.1〜§53.6 の実装仕様
- `docs/DEV_SETUP_CHECKLIST.md` §1.4 / §4.6 / §13 — Windows native gate
- `src/lib/bindings.ts` — specta 化 4 cmd + ProductWithRelations 型
- `src/lib/query-keys.ts` — queryKeys helper (commit 5a5f2fa 既導入)
- `src/lib/invoke.ts` — typedInvoke / unwrapResult
- `src/config/navigation.ts` — ActionButton SSOT
- `src/routes/index.tsx` — commit 5 で差し替え対象 (現状 93 行 → 3 行)
- `src/components/ui/alert.tsx` — Step 2 で新規追加

## Verification (end-to-end)

PR open 前 (Step 5-7) に以下を全通過させる:

```bash
# Backend (WSL2)
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test                                             # 561 本維持
cargo test --test architecture_test
cargo test --test design_compliance_test
cargo run --bin generate_bindings && git diff --exit-code src/lib/bindings.ts

# Docs / scripts
./scripts/doc-consistency-check.sh                     # 19 項目
./scripts/check-typedinvoke-count.sh                   # baseline 0

# Frontend (WSL2)
npm run typecheck
npm run lint
npm run format:check
npm run build

# Manual (Windows native)
npm run tauri dev   # 検証 13-18 を全部通す
```

セルフレビュー (Step 7) で `inventory-code-review` skill を `git diff main...HEAD` に適用し P1/P2 ゼロを確認後、PR open。

## Out of Scope (Backlog)

- Phase 2 8-6 ショートカット一覧ダイアログ (Ctrl+/) — UI-00 merge 後の別 PR
- Phase 2 8-7 ファイルエクスポート後 UX 共通化 — UI-09a/b で実装
- 本格的な日本語 IME 検証 — UI-07 / UI-01a 着手時
- 手芸店実機での MSI 動作確認 — Phase 2 完了 gate (`v0.8.0-ui-daily` タグ)

## Self-Review

memory `plan-self-review-before-implementation.md` の 7 観点で実施 (2026-05-09)。AskUserQuestion 1 回 + Edit 反映 6 回で抜け漏れ潰し済。

1. **技術的前提**: LSP/Skills Policy hook 必須手順を Step 3/4 に inline 反映 / rebase 判定を Step 8 事前判定に追加 / commit prefix (feat / chore / docs / fix / refactor) を各 Step に明記
2. **スクリプト詳細**: Step 5 を「グループ A (Rust 順次) / B (Frontend 順次) / C (Scripts 独立) — グループ間のみ並列」に明確化 / Step 8 で pre-push hook 4 ステップ (fmt/clippy/test → doc-consistency → typedinvoke → env-safety) と各 fail 時の対処を明記
3. **ドキュメント修正**: `docs/function-design/53-ui-home.md` / `FUNCTION_DESIGN.md` 更新履歴反映を Step 4 に追加 / archive 移動時の相対パス変換 (`../` → `../../`) を Step 11 で明記 (memory `feedback-archive-relative-path-conversion.md`)
4. **検証計画**: Step 6 の `VITE_MOCK_MODE` 不確実性に対する fallback (DB 一時 rename) を明記 / Step 5 で test 561 本維持 / `cargo run --bin generate_bindings` の drift check を Step 5 に明記
5. **後処理**: Step 11 末尾に memory 軽量監査 + `.last_audit` sentinel 更新を追加 / archive 移動 + Plans.md Backlog 更新 + DEV_SETUP_CHECKLIST §13.1 gate 更新を分離して列挙
6. **実行制約**: 「実行制約 (全 step 共通)」セクションを新設 / merge / force push / Codex 依頼は user 操作と明記 / 中間 push 方針 (commit 4 → 5 を 1 push にまとめる) を明記
7. **コミット分割**: commit 4 を分割しない判断 + Alert 独立 commit 3.5 + Plans.md 同期は boundary milestone 1 commit を「主要判断 (確定)」表に明記 / 各 Step に commit message prefix と内容を明記

**新規追加 Step 0**: hook 改修でセルフレビュー漏れを構造的に止める仕組みを追加 (今回の漏れの根本原因対処)。Step 0 完了後は本 plan のような Self-Review セクションが ExitPlanMode で機械検証される。

### 2026-05-09 追加レビュー反映 (11 件)

次セッションで本プランを精読した結果、新たに 11 件の改善余地を検出 (P1 1 件 / P2 3 件 / P3 7 件)。実行前に Edit で全件反映。

| ID | 種別 | 反映内容 |
|---|---|---|
| P1-A | ブロッカー | Step 11 で本プラン (`2026-05-09-phase-2-ui-00-commit-4-5.md`) 自体の archive 移動を明記 |
| P2-A | デザイン矛盾 | `useYesterdayDate` 再評価メカニズムを Visibility API listener 版に明確化（53-ui-home.md §53.3 + phase-2-ui-00.md L90 連携）|
| P2-B | 責務曖昧 | `count-stock-status.ts` 純関数仕様を 53-ui-home.md §53.2 末尾に明記 + 派生値表に「計算箇所」列追加 |
| P2-C | 論理破綻 | sales クエリ `enabled` を `yesterday !== null` から `true` に変更 + 根拠注釈追記 |
| P3-A | 状態 mark 漏れ | Step 0 タイトルに `[完了済 / commit 4ab7017]` mark + Context 注記 |
| P3-B | 並行明確化 | ステップ依存関係セクション新設 + Step 1 タイトルに deadline 明記 + 並行運用ガイド |
| P3-C | 確認順序 | Step 2 末尾に `VITE_MOCK_MODE` 確認の前倒し + Step 6 事前確認注記更新 |
| P3-D | 同期作業漏れ | Step 10 で Plans.md Backlog L142 specta 化リスト更新を明記 |
| P3-E | 運用明確化 | Step 11 archive 移動 commit の push 先 (main 直接 commit) を明記 |
| P3-F | テスト方針 | 53-ui-home.md §53.9 非目的に Vitest unit test 行追加 |
| P3-G | サポート明示 | Step 1 に Claude サポート範囲 / user 操作範囲表 + フロー追加 |
